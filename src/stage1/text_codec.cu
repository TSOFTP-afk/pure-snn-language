// text_codec.cu - text <-> spike codec (placeholder, implemented in stage 1b)
// Current stage 1a only does gradient check + synthetic task, no real text codec needed

// Future implementation:
//   text_to_spikes(char c, int n_neurons, int t_steps) -> spike train
//   spikes_to_text(float* spike_rates, int n_neurons) -> char

void text_codec_placeholder() {
    // placeholder function to avoid empty compilation unit
}
