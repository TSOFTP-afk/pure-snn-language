# 阶段 2：联合皮层发育 - 实施规划（v3，基于代码逐行核对）

> v2 的"基于真实代码状态"是错的——v2 没有逐行读代码，仍含多个臆测。v3 基于 `src/include/*.h`、`src/cuda/*.cu`、`src/host/*.cpp`、`src/stage1/*` 的逐行核对重写，所有"复用/修改/新建"决策都有代码行号支撑。

---

## 0. 真实代码基础（逐行核对结果）

### 0.1 stage0 代码可复用性（逐文件核对）

| 文件 | 关键内容 | 阶段 2 处置 |
|------|---------|------------|
| `include/config.h:9-14` | 神经元 2k+6k+2k=10k，突触 1M，CSR 格式 | **修改**：加阶段 2 开关，不改 stage0 默认值 |
| `include/config.h:17-21` | LIF: β=0.95, θ=1.0, reset=0, refractory=2 | 直接复用 |
| `include/config.h:23-29` | STDP: A±=0.05, τ=20ms, W∈[0,1] | 直接复用 |
| `include/config.h:36-40` | Homeostatic: 感觉/联合 5Hz, 运动 30Hz | 直接复用 |
| `include/types.h:27-37` | `NeuronState` 24 字节，含 `threshold_offset` | 直接复用 |
| `include/types.h:46-55` | `Synapse` 32 字节，含 STDP 状态 + eligibility | 直接复用 |
| `include/types.h:87-126` | `get_region()`、`get_neuron_type()` 等 inline 函数 | 直接复用 |
| `include/neuron.cuh` | `init_neurons`、`lif_update`、`update_fire_rate`、`reset_neurons` | 直接复用 |
| `include/synapse.cuh:12-29` | `synaptic_transmission_csr_kernel`（CSR 突触传播） | 直接复用 |
| `include/synapse.cuh:38-42` | `sync_weights_kernel`（d_synapses→d_weights 同步） | 直接复用 |
| `include/stdp.cuh:12-19` | `stdp_update_kernel`（含 dopamine 调制） | 直接复用 |
| `include/network.h:11-68` | `SNNNetwork` 类，**所有 device 指针都是 private** | **必须修改**（见 0.2） |
| `include/network.cuh:25-26` | `init_synapses()`（均匀随机拓扑） | stage2 不调用 |
| `include/io.cuh:11-20` | `inject_input`、`read_output`、`compute_stats` | 直接复用 |
| `include/trainer.h:36-73` | `Trainer` 类，4 模式识别任务硬编码 | stage2 不用 |
| `cuda/neuron_kernel.cu` | LIF + 内嵌 homeostatic（`threshold_offset` 调节） | 直接复用 |
| `cuda/stdp_kernel.cu` | STDP + eligibility trace + dopamine 调制 | 直接复用 |
| `cuda/synapse_kernel.cu` | CSR 突触传播 + 权重同步 | 直接复用 |
| `cuda/network_init.cu:31-113` | 均匀随机拓扑（联合皮层 50% 感觉 + 50% recurrent） | stage2 不调用 |
| `cuda/io_kernel.cu` | 输入注入 + 输出读取 + 统计 | 直接复用 |
| `host/network.cpp:71-96` | `SNNNetwork::initialize()` 写死调用 `init_synapses()` | **必须修改**（见 0.2） |
| `host/network.cpp:104-141` | `step()` 内部 8 步流水线（clear→syn→inj→lif→stdp→sync→rate→stats） | 直接复用 |
| `host/network.cpp:174-196` | `save_weights()` 只保存 d_weights_（4MB），不保存 d_synapses_ | **stage2 不用**（见 0.3） |
| `host/trainer.cpp` | 4 模式识别训练循环 | stage2 不用 |
| `host/main.cpp` | stage0 入口 | stage2 不用 |
| `host/io.cpp`、`host/monitor.cpp` | I/O 辅助 | 直接复用（改日志路径） |
| `CMakeLists.txt` | VS 生成器 + 全局 `CMAKE_CUDA_ARCHITECTURES` | stage2 不用此 CMakeLists |
| `scripts/build.ps1:69` | `cmake -G "Visual Studio 17 2022" -A x64` | stage2 改用 Ninja |

### 0.2 SNNNetwork 类的 private 问题（必须修改）

**问题**（`network.h:46-56`）：
```cpp
private:
    NeuronState* d_neurons_;
    Synapse*     d_synapses_;
    int*         d_row_ptr_;
    int*         d_col_idx_;
    float*       d_weights_;
    // ... 全部 private，无 getter
```

**影响**：stage2 的 `columnar_topology.cu` 无法直接填充这些指针。

**解决方案（3 选 1）**：

**方案 A（推荐）**：给 SNNNetwork 加 public getter + 一个新的 `initialize_with_custom_topology()` 方法
```cpp
public:
    // 暴露 device 指针（const 版本，防止外部修改指针本身）
    NeuronState* get_d_neurons()       { return d_neurons_; }
    Synapse*     get_d_synapses()      { return d_synapses_; }
    int*         get_d_row_ptr()       { return d_row_ptr_; }
    int*         get_d_col_idx()       { return d_col_idx_; }
    float*       get_d_weights()       { return d_weights_; }
    float*       get_d_input_current() { return d_input_current_; }
    bool*        get_d_spikes()        { return d_spikes_; }

    // 新增：分配显存但不初始化拓扑（让外部填充）
    void allocate_only();
```

**方案 B**：把 `initialize()` 拆为 `allocate_memory()` (public) + `init_topology_default()` (private)，stage2 调用前者 + 自己的拓扑初始化。

**方案 C**：stage2 不用 SNNNetwork 类，自己管理 device 内存（彻底独立，但代码重复）。

**推荐方案 A**——改动最小，不影响 stage0 既有调用（`initialize()` 仍可用）。

### 0.3 stage0 的 save_weights 不够用

**问题**（`network.cpp:174-196`）：`save_weights()` 只保存 `d_weights_`（4MB float 数组），不保存 `d_synapses_`（32MB，含 `last_pre_spike`、`last_post_spike`、`eligibility` 等 STDP 状态）。

**影响**：stage2 的 checkpoint 必须能恢复 STDP 训练状态，否则 resume 后 STDP 时序信息丢失。

**解决**：stage2 自己实现 `save_checkpoint()` / `load_checkpoint()`，直接保存/加载 `d_synapses_` 完整结构（32MB）。

### 0.4 stage1 的可复用部分

| 文件 | 内容 | 阶段 2 处置 |
|------|------|------------|
| `stage1/CMakeLists.txt` | Ninja 构建 + target 级 `CUDA_ARCHITECTURES "86"` + UTF-8 处理 | **作为 stage2 CMake 模板** |
| `stage1/text_codec.cu` | 5-bit / 32 字符（A-Z + 空格标点），`__host__` 函数 | **扩展为 7-bit / 95 可打印 ASCII** |
| `stage1/bptt_*.cu` | 全连接 N=10 BPTT demo | **不复用**（与 CSR 稀疏 10k 不兼容） |
| `stage1/main.cpp` | stage1 入口 | 不用 |

### 0.5 已确认的环境约束（不变）
- CUDA 13.3 + MSVC 19.44 + RTX 3060 (sm_86) + 6GB 显存
- VS DevShell 必须 `-HostArch amd64 -Arch amd64`（否则 cudafe++ 崩溃）
- 源码全英文（中文注释/字符串触发 cudafe++ 词法分析问题）
- stage0 实际显存占用 38MB（10k 神经元 + 1M 突触），10⁵ 规模预估约 380MB（远低于 6GB）

### 0.6 用户已确认的决策
- 起步规模：10⁴ 起步迭代
- 数据源：真实对话子集（DailyDialog）
- 分析工具：纯 CUDA 自实现
- Baseline：随机 SNN + 消融实验

---

## 1. 阶段 2 目标与判据（工程方案 5.4 节原文）

### 1.1 目标
无监督 STDP 在联合皮层产生有意义的结构。不优化对话输出，只观察联合皮层内部结构。

### 1.2 验收判据（工程方案原文，四项并行）
1. 联合皮层出现有意义的内部表征聚类（用 t-SNE/UMAP 可视化）
2. 聚类与对话语义相关（如不同话题对应不同聚类）
3. 神经元发放率分布符合生物数据（power-law，少数神经元高发放）
4. 与 baseline（随机初始化网络）对比，结构显著性检验 p < 0.01

### 1.3 工程化阈值（纯 CUDA 自实现工具调整）
| # | 判据 | 测量方法 | 通过阈值 |
|---|------|---------|---------|
| 1 | 表征聚类涌现 | CUDA PCA（前 10 维）+ K-means（K=3..10） | silhouette > 0.3 且最优 K ≥ 3 |
| 2 | 聚类与语义相关 | 字符/词频率 × 聚类标签的卡方检验 | p < 0.01 |
| 3 | 发放率 power-law | CUDA 最小二乘拟合 + KS 检验 | p < 0.05，α ∈ [1.5, 3.0] |
| 4 | 显著优于 baseline | Train vs 随机 SNN，5 seeds，双样本 t 检验 | p < 0.01，Cohen's d > 0.5 |

### 1.4 停损条件
- 阶段 2 三次失败 → 中止项目（结构同构假设受挫）
- 调质无效 → 重新设计调制规则（10.9）

---

## 2. 核心策略：从 stage0 出发的最小侵入式改造

### 2.1 改动范围
- **修改 stage0 代码**：仅 `network.h`（加 getter）+ `network.cpp`（加 `allocate_only()`）
- **新增 stage2 代码**：在 `src/stage2/` 独立目录，通过 include 复用 stage0 头文件 + 链接 stage0 的 .cu/.cpp
- **不修改**：stage0 的 LIF/STDP/CSR/io kernel、stage1 任何代码

### 2.2 代码组织
```
src/
├── include/                    # stage0 头文件
│   ├── config.h                # ← 不改（stage2 用自己的 config）
│   ├── network.h               # ← 改：加 public getter + allocate_only()
│   ├── types.h、neuron.cuh、synapse.cuh、stdp.cuh、io.cuh  # ← 不改
│   ├── network.cuh、trainer.h  # ← 不改（stage2 不用 trainer）
├── cuda/                       # stage0 kernels
│   ├── neuron_kernel.cu、stdp_kernel.cu、synapse_kernel.cu、io_kernel.cu  # ← 复用
│   └── network_init.cu         # ← stage2 不链接此文件
├── host/                       # stage0 host
│   ├── network.cpp             # ← 改：加 allocate_only() 实现
│   ├── io.cpp、monitor.cpp     # ← 复用
│   └── trainer.cpp、main.cpp   # ← stage2 不链接
├── stage2/                     # 新增
│   ├── CMakeLists.txt          # 新建（Ninja，链接 stage0 的 .cu/.cpp）
│   ├── main.cpp                # 新建（stage2 入口）
│   ├── config.h                # 新建（stage2 专用配置：柱数、p_intra、p_inter 等）
│   ├── columnar_topology.cu    # 新建（皮层柱拓扑生成器）
│   ├── columnar_topology.cuh   # 新建
│   ├── text_codec_ext.cu       # 新建（7-bit / 95 可打印 ASCII 扩展）
│   ├── text_codec_ext.cuh      # 新建
│   ├── text_stream.cu          # 新建（DailyDialog 字符流注入器）
│   ├── text_stream.cuh         # 新建
│   ├── unsupervised_trainer.cu # 新建（100 万步训练循环 + checkpoint）
│   ├── unsupervised_trainer.cuh# 新建
│   └── analysis/               # 新建（纯 CUDA 结构分析）
│       ├── pca.cu、kmeans.cu、power_law.cu、silhouette.cu、chi_square.cu
│       └── *.cuh
└── stage1/                     # 已完成，保留
```

### 2.3 stage2 CMakeLists.txt 设计
```cmake
cmake_minimum_required(VERSION 3.18)
project(snn_stage2 LANGUAGES CXX CUDA)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CUDA_STANDARD 17)

# UTF-8 处理（与 stage1 一致）
add_compile_options($<$<COMPILE_LANGUAGE:CXX>:/utf-8>)
add_compile_options($<$<COMPILE_LANGUAGE:CUDA>:-Xcompiler=/utf-8>)

find_package(CUDAToolkit REQUIRED)

add_executable(snn_stage2
    # stage2 新增
    main.cpp
    columnar_topology.cu
    text_codec_ext.cu
    text_stream.cu
    unsupervised_trainer.cu
    analysis/pca.cu
    analysis/kmeans.cu
    analysis/power_law.cu
    analysis/silhouette.cu
    analysis/chi_square.cu
    # stage0 复用（不含 network_init.cu、trainer.cpp、main.cpp）
    ../cuda/neuron_kernel.cu
    ../cuda/stdp_kernel.cu
    ../cuda/synapse_kernel.cu
    ../cuda/io_kernel.cu
    ../host/network.cpp
    ../host/io.cpp
    ../host/monitor.cpp
)

target_include_directories(snn_stage2 PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_CURRENT_SOURCE_DIR}/../include
    ${CUDAToolkit_INCLUDE_DIRS}
)

target_compile_options(snn_stage2 PRIVATE
    $<$<COMPILE_LANGUAGE:CUDA>:-O3>
    $<$<COMPILE_LANGUAGE:CUDA>:--ptxas-options=-v>
)

set_target_properties(snn_stage2 PROPERTIES CUDA_ARCHITECTURES "86")
```

**关键点**：
- 链接 stage0 的 4 个 .cu（不含 `network_init.cu`）+ 3 个 .cpp（不含 `trainer.cpp`、`main.cpp`）
- 这样 stage2 可直接调用 `SNNNetwork`、`lif_update`、`stdp_update` 等
- stage2 不链接 `network_init.cu`，所以 `init_synapses()` 符号不出现——`SNNNetwork::initialize()` 会链接失败
- **解决方案**：stage2 不调用 `initialize()`，改调用新增的 `allocate_only()` + stage2 自己的 `init_columnar_synapses()`

---

## 3. 子阶段分解

### 2a：皮层柱拓扑 + 数据流（最小可行验证）

**目标**：在 10⁴ 规模下用皮层柱拓扑替换 stage0 均匀随机拓扑，跑通"文字流→脉冲→STDP→活动快照"管线。

**任务清单**：

1. **修改 stage0 的 `network.h` + `network.cpp`**：
   - 加 public getter（`get_d_synapses()` 等，见 0.2 方案 A）
   - 加 `void allocate_only()` 方法：只分配显存 + 初始化神经元 + 清零缓冲区，**不调用 `init_synapses()`**
   - `initialize()` 保持不变（仍调用 `init_synapses()`，stage0 路径不受影响）

2. **新建 `stage2/config.h`**：
   ```cpp
   #define STAGE2_N_COLUMNS         6      // 联合皮层柱数
   #define STAGE2_NEURONS_PER_COL   1000   // 每柱神经元数（6×1000=6000，与 stage0 一致）
   #define STAGE2_P_INTRA           0.1f   // 柱内连接概率
   #define STAGE2_P_INTER           0.005f // 柱间连接概率
   #define STAGE2_N_INPUT_NEURONS   7      // 感觉皮层前 7 个神经元用于字符注入
   #define STAGE2_CHAR_DURATION     10     // 每字符持续 K 步
   #define STAGE2_TOTAL_STEPS       1000000  // 100 万步
   #define STAGE2_CHECKPOINT_INTERVAL 10000
   ```

3. **新建 `stage2/columnar_topology.cu`**：
   - 函数 `init_columnar_synapses(Synapse*, int*, int*, float*, unsigned int seed)`
   - 签名与 stage0 `init_synapses()` 一致（直接填 SNNNetwork 的 device 指针）
   - 拓扑规则：
     - 感觉皮层（0..1999）：自连接（recurrent，用作记忆回路）
     - 联合皮层（2000..7999）：分 6 柱，每柱 1000 神经元
       - 柱内 pre：~95 个（p_intra=0.1 × 1000 ≈ 100，减去自连接）
       - 柱间 pre：~5 个（p_inter=0.005 × 5000 ≈ 25，限制为 5 个长程）
       - 跨脑区 pre：来自感觉皮层（投影）
     - 运动皮层（8000..9999）：来自联合皮层（投影）
   - 权重初值：兴奋 pre → 正权重 [0, 0.3]，抑制 pre → 负权重 [-0.3, 0]
   - 用 `get_neuron_type()` 判断兴奋/抑制（与 stage0 一致）

4. **新建 `stage2/text_codec_ext.cu`**：
   - 扩展 stage1 的 5-bit/32 字符为 7-bit/95 字符
   - 字符集：ASCII 0x20-0x7E（95 个可打印字符：空格、标点、数字、大小写字母）
   - 编码：`char_to_bits_7(char c, float bits[7])` — idx = c - 0x20，7-bit 二进制
   - 解码：`bits_to_char_7(const float bits[7])` — 阈值 0.25
   - **注意**：是 `__host__` 函数（与 stage1 一致，编解码在 host 端做）

5. **新建 `stage2/text_stream.cu`**：
   - 加载 DailyDialog 子集文本（~10MB，纯 ASCII）
   - 逐字符读出，调用 `char_to_bits_7()` 编码
   - 生成 `float[N_SENSORY_NEURONS]` 输入向量：
     - 前 7 位 = 字符的 7-bit 编码 × 1.5（gain）
     - 其余 1993 位 = 0
   - 每字符持续 K=10 时间步（`STAGE2_CHAR_DURATION`）

6. **新建 `stage2/unsupervised_trainer.cu`**（骨架）：
   - 主循环：100 万步，每 K 步切换一个字符
   - 每步：调用 `text_stream.get_input(h_input, step)` → `network.step(h_input, step)`
   - 每 1 万步：保存 checkpoint（d_synapses_ 完整 32MB）+ 导出 rate_map CSV
   - **stage2 主线不用 reward**（工程方案 5.4 说"不优化对话输出"）——只跑纯 STDP + homeostatic
   - reward 作为消融实验的可选项（B3 实验时再加）

7. **新建 `stage2/main.cpp`**：
   - 创建 SNNNetwork → `allocate_only()` → `init_columnar_synapses()` → 跑 1 万步验证

**验收 2a**：
- 编译通过，1 万步运行不崩
- 感觉皮层前 7 个神经元的 fire_rate 反映字符注入模式（非全零）
- 联合皮层至少 10% 神经元发放（信号从感觉传播到联合）
- checkpoint 文件 + rate_map CSV 正确生成

### 2b：100 万步 STDP 训练 + 结构观察

**目标**：完整运行 100 万时间步，观察结构涌现趋势。

**任务清单**：
1. 准备 DailyDialog 子集（10MB 无标注英文对话，纯 ASCII）
2. STDP 参数沿用 stage0（A±=0.05, τ=20ms），homeostatic 沿用（5Hz/30Hz）
3. 启用 eligibility trace + dopamine（dopamine_base=1.0，无外部 reward）
4. 运行 100 万步（预估 3-7 天，每 1 万步 checkpoint）
5. 每 10 万步导出结构演化快照：
   - 权重分布直方图（观察是否多峰）
   - 发放率分布（观察是否趋向 power-law）
   - 联合皮层 PCA 前 2 维轨迹

**验收 2b**：
- 100 万步完成无崩溃
- 发放率分布非均匀（Gini > 0.3）
- 权重分布出现多峰结构（非单峰高斯）

### 2c：结构分析 + Baseline + 消融

**目标**：定量验证四项判据。

**任务清单**：
1. 实现纯 CUDA 结构分析工具（`stage2/analysis/*.cu`）：
   - `pca.cu`：协方差矩阵 + Jacobi 特征分解（前 10 维）
   - `kmeans.cu`：Lloyd 算法，K=3..10 搜索
   - `power_law.cu`：最小二乘拟合 α + KS 检验
   - `silhouette.cu`：聚类质量
   - `chi_square.cu`：聚类 vs 字符/词频率相关性
2. 对 2b 训练后的网络做四项判据评估
3. Baseline + 消融实验（5 seeds 每组）：
   - **Train**：完整皮层柱 + STDP + homeostatic + 真实对话
   - **B1 随机**：相同拓扑，仅 forward 100 万步，无 STDP
   - **B2 无 STDP**：训练框架运行但禁用 STDP 更新（`stdp_update` 调用前 return）
   - **B3 加 Reward**：Train 基础上再加 reward 信号（验证 reward 是否增强结构涌现）
   - **B4 无 Homeo**：禁用 homeostatic（`threshold_offset` 固定为 0）
4. 统计检验：Train vs B1，双样本 t 检验，p < 0.01，Cohen's d > 0.5
5. Bonferroni 校正（4 组 × 4 判据 = 16 次检验，α_adj ≈ 0.003）

**验收 2c**：四项判据全部通过（见 1.3 节）

### 2d（条件触发）：扩展到 10⁵
**触发条件**：2c 通过且显存 < 1GB（实际预估 380MB，远低于 6GB）。

**任务清单**：
1. 联合皮层从 6000 扩展到 100000（约 16× 规模，柱数从 6 → 100）
2. 突触数从 1M 扩展到 ~10M
3. 显存预估：神经元 2.4MB + 突触 320MB + CSR 80MB ≈ 400MB（仍远低于 6GB）
4. 重新跑 100 万时间步
5. 验证判据在 10⁵ 规模下仍成立

---

## 4. 关键技术方案

### 4.1 皮层柱拓扑（解决 stage0 信号稀释教训）

**stage0 失败原因**（`network_init.cu:71-78`）：
- 联合皮层 pre 50% 来自感觉（均匀随机选 2000 个中的任意一个）
- 50% 来自联合（均匀随机选 6000 个中的任意一个）
- 中心极限定理 → 每个联合神经元收到的总输入统计上相同 → 跨模式无差异

**stage2 皮层柱方案**：
```
感觉皮层 (2000) ──投影──→ 联合皮层 (6000, 6 柱 × 1000) ──投影──→ 运动皮层 (2000)
                            │  │  │  │  │  │
                            └──┴──┴──┴──┴──┘ 柱间长程稀疏 (p=0.005)
                            柱内局部密集 (p=0.1)

每柱 1000 神经元 = 800 兴奋 + 200 抑制（沿用 get_neuron_type()）
柱内突触：每神经元 ~95 个（局部 recurrent）
柱间突触：每神经元 ~5 个（长程稀疏）
跨脑区突触：每神经元 ~0 个（联合→运动单独投影）
```

**为什么能解决信号稀释**：
- 字符注入感觉皮层前 7 个神经元 → 信号经感觉→联合投影传播
- 联合皮层不同柱接收不同感觉神经元子集的投影 → 柱间出现差异化激活
- 柱内 recurrent 局部放大 → 信号在柱内增强而非被中心极限定理平均掉

### 4.2 数据流
```
DailyDialog 文本（10MB，纯 ASCII）
    ↓
text_stream 逐字符读出（host 端）
    ↓
text_codec_ext 编码为 7-bit（host 端，__host__ 函数）
    ↓
生成 float[2000] 输入向量（前 7 位 = 编码 × 1.5，其余 0）
    ↓
network.step(h_input, step)（每字符持续 K=10 步）
    ↓
联合皮层 STDP 发育（100 万步）
    ↓
每 1 万步 checkpoint（d_synapses_ 完整 32MB）+ rate_map CSV
    ↓
结构分析（PCA + K-means + power-law + 卡方）
```

### 4.3 训练配置
| 参数 | 值 | 来源 |
|------|----|----|
| 总时间步 | 100 万 | 工程方案 5.4 |
| STDP 更新频率 | 每步 | 工程方案 5.4 |
| A_plus / A_minus | 0.05 | stage0 修复版 |
| τ_plus / τ_minus | 20 ms | 工程方案 2.2.2 |
| Dopamine base | 1.0（无外部 reward） | 工程方案 5.4 "不优化对话输出" |
| Homeostatic 目标 | 感觉/联合 5Hz，运动 30Hz | stage0 修复版 |
| Checkpoint 频率 | 每 1 万步 | 自定 |
| 快照导出频率 | 每 1 万步（活动）/ 每 10 万步（结构） | 自定 |

**关于 reward**（修正 v2 的错误）：
- 工程方案 5.4 原文："不优化对话输出，只观察联合皮层内部结构"
- stage0 的 reward 计算依赖 motor 输出匹配目标模式，stage2 无目标模式
- **决策**：stage2 主线跑纯 STDP + homeostatic + dopamine_base=1.0（无外部 reward）
- reward 作为 B3 消融实验的可选项（验证加 reward 是否增强结构涌现）

**关于替代梯度辅助**：
- stage1 的 BPTT 是全连接 N=10，与 CSR 稀疏 10k 完全不兼容
- 重写 BPTT 适配 CSR + 10k 工作量巨大
- **决策**：阶段 2a-2c 先纯 STDP，2c 失败再考虑 BPTT 辅助

### 4.4 结构分析工具（纯 CUDA 自实现）
| 工具 | 算法 | 复杂度 | 输入 | 输出 |
|------|------|--------|------|------|
| PCA | 协方差矩阵 + Jacobi 特征分解 | O(N²) | 联合皮层活动矩阵 [T×N] | 前 10 维特征向量 + 解释方差比 |
| K-means | Lloyd 算法（CUDA 并行） | O(NKd × iter) | PCA 降维后 [N×10] | 聚类标签 + 中心 |
| power-law | 最小二乘 + KS 检验 | O(N log N) | 神经元发放率 [N] | α + p-value |
| silhouette | 距离矩阵 + 聚类内/间均值 | O(N²) | 降维数据 + 聚类标签 | silhouette 分数 |
| 卡方检验 | 联列表 + df=(K-1)(V-1) | O(KV) | 聚类标签 × 字符频率 | χ² + p-value |

**降维策略**：PCA 前 10 维足够（N=6000 神经元活动降到 10 维），不用 t-SNE。

---

## 5. Baseline 与消融实验（修正 v2 的错误）

### 5.1 实验矩阵（修正：B3 改为"加 Reward"，因为主线无 reward）
| 组别 | 拓扑 | STDP | Reward | Homeostatic | 数据 | 用途 |
|------|------|------|--------|-------------|------|------|
| **Train** | 皮层柱 | ✓ | ✗ | ✓ | DailyDialog | 主实验（纯 STDP + homeo） |
| B1 随机 | 皮层柱 | ✗ | ✗ | ✗ | 无 | 下界 |
| B2 无 STDP | 皮层柱 | ✗ | ✗ | ✓ | DailyDialog | 证明 STDP 必要 |
| B3 加 Reward | 皮层柱 | ✓ | ✓ | ✓ | DailyDialog | 验证 reward 是否增强涌现 |
| B4 无 Homeo | 皮层柱 | ✓ | ✗ | ✗ | DailyDialog | 证明稳态必要 |

### 5.2 统计检验
- 每组 5 个不同 seed
- Train vs B1：双样本 t 检验，p < 0.01
- 效应量：Cohen's d > 0.5
- Bonferroni 校正（4 组 × 4 判据 = 16 次检验，α_adj ≈ 0.003）

---

## 6. 风险与停损

### 6.1 主要风险
| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| 皮层柱仍信号稀释 | 中 | 2b 失败 | 调柱数/连接概率，或加 lateral inhibition |
| 100 万步训练耗时过长 | 高 | 迭代慢 | 先 10 万步看趋势再决定是否继续 |
| power-law 不成立 | 中 | 判据 3 失败 | 放宽 α 范围或换 heavy-tail 检验 |
| STDP 不产生聚类 | 中 | 阶段失败 | 2c 失败后引入 BPTT 辅助（替代梯度回退方案） |
| 10⁵ 扩展显存不足 | 低 | 2d 无法进行 | 预估 400MB，实际远低于 6GB |

### 6.2 失败归因
1. **假设证伪**：结构同构假设不成立 → 中止项目
2. **工程问题**：拓扑/参数/数据不当 → 调整后重试（最多 3 次）
3. **规模不足**：10⁴ 太小无法涌现 → 扩展到 10⁵

---

## 7. 待决策点

1. **SNNNetwork 改造方案**（见 0.2）：
   - 推荐**方案 A**：加 public getter + `allocate_only()` 方法
   - 影响：仅修改 `network.h` + `network.cpp`，stage0 既有调用不变

2. **DailyDialog 数据获取**：
   - 选项 A：手动下载 http://parl.ai/projects/dailydialog/ 的 zip
   - 选项 B：写 PowerShell 脚本下载
   - 推荐 B（可重复构建）

3. **Checkpoint 格式**：
   - 选项 A：裸二进制（直接 memcpy d_synapses_ 32MB）
   - 选项 B：分文件（d_synapses_ + d_weights_ + d_neurons_ 分开）
   - 推荐 A（与 stage0 save_weights 风格一致，但保存 d_synapses_ 而非 d_weights_）

4. **运动皮层是否参与**：
   - 选项 A：保留（为阶段 3 衔接，仅 2000 神经元不占多少显存）
   - 选项 B：移除（节省显存，但破坏 stage0 SNNNetwork 类的假设）
   - 推荐 A（保留衔接性，运动皮层仅 2000 神经元）

5. **构建系统**：
   - stage2 用 Ninja（与 stage1 一致，已验证可用）

6. **字符编码扩展方案**：
   - 选项 A：7-bit / 95 可打印 ASCII（0x20-0x7E）
   - 选项 B：8-bit / 128 ASCII（含控制字符，不可打印）
   - 推荐 A（只编码可打印字符，覆盖英文对话足够）

---

## 8. 阶段 2 完成判据检查表

- [ ] stage2 目录搭建完成，CMake + Ninja 编译通过
- [ ] SNNNetwork 加 getter + `allocate_only()`，stage0 既有功能不受影响
- [ ] columnar_topology.cu 替换 stage0 拓扑，1 万步运行不崩
- [ ] text_codec_ext + text_stream 字符流注入正常（前 7 个感觉神经元反映字符）
- [ ] 100 万时间步 STDP 训练完成
- [ ] PCA + K-means 显示聚类（silhouette > 0.3）
- [ ] 聚类与对话语义相关（卡方 p < 0.01）
- [ ] 发放率 power-law 分布（KS p < 0.05）
- [ ] Train vs B1 显著性（t 检验 p < 0.01，d > 0.5）
- [ ] 消融实验完成（B2/B3/B4 至少一组性能显著下降）
- [ ] 5 seeds 重复方向一致
- [ ] 提交到 `stage-2-association-cortex` 分支

---

## 9. v3 vs v2 的修正清单

| # | v2 错误 | v3 修正 |
|---|---------|--------|
| 1 | "stage2 直接操作 SNNNetwork device 指针" | device 指针全 private，必须加 getter（0.2） |
| 2 | "stage2 调用自己的 init_columnar_synapses()" | `initialize()` 写死调用 `init_synapses()`，必须加 `allocate_only()`（0.2） |
| 3 | "text_codec 扩展为 7-bit / 128 ASCII" | 128 含控制字符，改为 7-bit / 95 可打印 ASCII（0.4、7.6） |
| 4 | "stage2 用 Ninja 构建模板（参考 stage1）" | 明确 stage1 用 target 级 `CUDA_ARCHITECTURES`，stage2 沿用（2.3） |
| 5 | "字符注入感觉皮层" | 感觉皮层 2000 个神经元，7-bit 编码只占前 7 个，其余 1993 个为 0（4.2） |
| 6 | "重写 trainer.cpp" | 不能重写（破坏 stage0），stage2 完全新建 `unsupervised_trainer.cu`（2.2） |
| 7 | "reward 用字符匹配度" | 工程方案说"不优化对话输出"，stage2 主线无 reward，reward 是 B3 消融可选项（4.3、5.1） |
| 8 | "Trainer 不暴露 generate_pattern_input" | stage2 的 text_stream 自己生成 `float[2000]`，传给 `network.step()`（4.2） |
| 9 | "10⁴ 规模显存 < 2GB" | stage0 实际 38MB，10⁵ 预估 380MB（0.5、6.1） |
| 10 | "stage0 均匀随机稀疏，无 recurrent" | 联合皮层有 50% recurrent 自连接，但无局部偏好（0.1、4.1） |
| 11 | 未说明 text_codec 是 host 还是 device | 明确是 `__host__` 函数，编解码在 host 端（0.4、4.2） |
| 12 | "checkpoint 裸二进制（memcpy d_weights_）" | d_weights_ 不含 STDP 状态，必须保存 d_synapses_ 完整 32MB（0.3） |
| 13 | "homeostatic 在 trainer.cpp" | homeostatic 内嵌在 `lif_update()` kernel，Trainer 只管 reward（0.1） |
