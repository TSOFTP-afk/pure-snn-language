# Pure SNN Language Stage 2e：3M 正式训练结果分析报告

## Material Passport

- Origin Skill: `academic-research-suite / experiment-agent`
- Origin Mode: `validate`
- Origin Date: `2026-07-26`
- Verification Status: `ANALYZED`
- Version Label: `stage2e_3m_validation_v1`
- 项目：`TSOFTP-afk/pure-snn-language`
- 训练阶段：Stage 2e
- 训练设备：NVIDIA DGX Spark
- 训练目标：3,000,000 steps
- 最终断点：`src/stage2e/checkpoints/ckpt_step3000000.snn2e`

## 1. 执行摘要

Stage 2e 正式训练已经完成 3,000,000 步。训练进程正常退出，最终断点完整性校验通过，日志中没有发现 CUDA error、OOM、NaN/Inf、fatal、segfault、checksum failure 或 dropped events。

本次结果同时包含两个层面：

1. **工程层面成功**：55,000 神经元、10.7M 突触的 Stage 2e 架构完成长程稳定训练，事件链路无丢失，显存占用低且断点可恢复。
2. **科学目标部分成功**：模型形成了强烈的中文 UTF-8 字节选择性和稳定的初步聚类，但 eligibility、PSW 成熟、P3 activity drive、工作记忆和前额叶活动没有形成，尚不能证明模型获得了上下文语言建模或文本生成能力。

最准确的定位是：

> 模型已经完成“稳定脉冲动力学 → 输入字节选择性 → 初步聚类表征”，但尚未完成“时间信用分配 → 稀疏语义竞争 → 工作记忆 → 上下文组合 → 语言生成”。

## 2. 数据来源与验证范围

本报告使用以下数据：

- 用户提供的真实中文文本模式 10K、20K、100K 训练截图；
- DGX Spark 上的 `training_3000000.log` 最终日志；
- `inspect_checkpoint.py --verify` 对 3M 最终断点的完整性校验结果；
- 训练期间记录的 2.78M–3.00M 语义指标；
- 最终权重分布、分层卡方统计、神经调质、CaMKII、eligibility、PSW、P3、工作记忆和门控指标。

本报告未进行独立复现实验，也没有第二随机种子或外部留出集，因此结果状态为 `ANALYZED`，不能标记为 `VERIFIED/REPRODUCIBLE`。

## 3. 训练与工程结果

| 指标 | 最终结果 | 判断 |
|---|---:|---|
| 训练步数 | 3,000,000 | 完成 |
| 神经元数 | 55,000 | 架构未改变 |
| 突触数 | 10,700,000 | 架构未改变 |
| 训练阶段 | MATURE | 正常 |
| 最终断点文件 | `ckpt_step3000000.snn2e` | 存在 |
| 断点文件大小 | 1,469,373,132 bytes，约 1.40 GiB | 正常 |
| payload checksum | `000000007e61d59b` | 校验通过 |
| 显存峰值 | 1,442 MiB | 远低于 32 GiB 配置预算 |
| 到达延迟事件 | 625,347,389,477 | 正常 |
| 分发延迟事件 | 625,347,978,385 | 正常 |
| dropped events | 0 | 通过 |
| burst ratio | 1.0161% | 通过 |
| 最大单步脉冲 | 2,325 | 动态范围存在 |
| 训练错误扫描 | 未发现关键错误 | 通过 |

训练监控窗口内速度约为 78.6 steps/s，100K 步约需 21.2 分钟。GPU 训练时利用率通常约 96%，进程显存约 1.62 GiB；日志记录的持久状态显存为 1.442 GiB。

### 3.1 累计脉冲统计溢出

最终日志显示：

```text
total_spikes=-1122741185
avg_spikes_per_step=-374.247070
```

这不是神经活动为负，而是 32 位有符号整数计数溢出。按无符号 32 位换算：

```text
2^32 - 1,122,741,185 = 3,172,226,111 spikes
3,172,226,111 / 3,000,000 ≈ 1,057.41 spikes/step
```

因此最终判据 `[7] 发放活动正常` 的 FAIL 是统计实现造成的假失败。真实平均脉冲高于阈值 10，且与 100K 时约 1,021 spikes/step 基本一致。

该问题影响最终汇总数字和自动判据，但没有证据表明它影响 GPU 内部动力学、权重、事件队列或断点数据。

## 4. 早期截图与 3M 结果对比

| 指标 | 10K | 20K | 100K | 3M | 长程变化 |
|---|---:|---:|---:|---:|---|
| silhouette | 0.2223 | 未提供 | 未提供 | 0.2161 | 约下降 2.8%，总体持平 |
| JS mean | 0.2866 | 未提供 | 未提供 | 0.0947 | 下降约 67.0% |
| col_ratio | 557,961 | 未提供 | 未提供 | 157,308 | 下降约 71.8%，仍异常大 |
| 平均脉冲/步 | 未提供 | 未提供 | 1,021 | 约 1,057 | 基本稳定 |
| L6 EMA | 7.42 | 未提供 | 未提供 | 后期约 7.1–8.0 | 基本稳定 |
| 语料字节种类 | 未提供 | 未提供 | 157/256 | 155/256 非零 bins | 基本稳定，口径可能不同 |
| 网络响应字节种类 | 未提供 | 未提供 | 108/256 | 无完全同口径最终值 | 不宜直接比较 |

### 4.1 分层 chi²

| 层级 | 10K | 20K | 3M 最终均值 | 3M/10K |
|---|---:|---:|---:|---:|
| L4 | 9,085 | 17,092 | 1,148,165 | 126.4× |
| L2/3 | 2,703 | 5,652 | 445,823 | 164.9× |
| L5 | 5,076 | 11,794 | 930,512 | 183.3× |
| L6 | 4,558 | 10,221 | 774,017 | 169.8× |

这些数字证明网络形成了非常强的输入选择性，但不能直接解释为学习速度提高了 126–183 倍。chi² 是累计统计量，会随着观察次数增加而自然增大。若要比较真正的学习速率，必须使用：

- 相同长度的固定验证窗口；
- 按每字节注入次数或样本数归一化的统计量；
- 固定留出语料；
- 多随机种子均值和置信区间。

截图中的“学习速度提升 2–6 倍”和“突破理论极限”应视为早期探索性描述，而不是已验证的科学结论。

## 5. 语义聚类趋势

| Step | Silhouette | JS mean | JS max | col_ratio |
|---:|---:|---:|---:|---:|
| 10K | 0.2223 | 0.2866 | 未提供 | 557,961 |
| 2.78M | 0.2141 | 0.0968 | 0.2643 | 160,991 |
| 2.79M | 0.2219 | 0.0967 | 0.2639 | 161,272 |
| 2.80M | -0.0268 | 0.0968 | 0.2634 | 160,653 |
| 2.81M | -0.0266 | 0.0967 | 0.2631 | 160,930 |
| 2.82M | -0.0266 | 0.0966 | 0.2627 | 160,762 |
| 2.83M | 0.2140 | 0.0963 | 0.2626 | 160,743 |
| 2.90M | 0.2013 | 0.0956 | 0.2603 | 159,614 |
| 2.96M | 0.2242 | 0.0950 | 0.2583 | 158,559 |
| 2.98M | 0.2247 | 0.0949 | 0.2574 | 157,725 |
| 3.00M | 0.2161 | 0.0947 | 0.2566 | 157,308 |

### 5.1 可确认的结论

- 最终 silhouette 为 0.2161，高于设计门槛 0.15，P3-C 聚类判据通过。
- 10K 的 silhouette 为 0.2223，3M 为 0.2161，长程训练没有带来明显净提升。
- 2.80M–2.82M 的负值已通过同一断点的多次 k-means 初始化检查定位为局部最优问题；其后指标自行恢复，底层响应和 JS 指标没有同步坍塌。
- 后期 silhouette 长期位于约 0.20–0.225，说明聚类结构存在且稳定，但进入明显平台期。
- JS mean 从 0.2866 降至 0.0947，低于原设计目标 0.15，说明柱间响应分布差异没有随训练增强，反而趋同。

### 5.2 不能据此证明的能力

Silhouette 只证明选定表征空间中存在一定可分性，不能单独证明：

- 模型理解词义或句义；
- 模型具有下一字节预测能力；
- 模型具有语言生成能力；
- 聚类由学习产生，而不是主要来自 UTF-8 编码和字节频率结构。

需要通过打乱文本、频率匹配随机序列、单字节循环、留出语料以及下游预测任务进行对照。

## 6. 分层选择性和输入响应

最终活跃神经元中的显著字节选择性如下：

| 层级 | 显著神经元 | 活跃神经元 | 显著/活跃 | chi² 均值 |
|---|---:|---:|---:|---:|
| L4 | 6,837 | 6,838 | 99.99% | 1,148,165 |
| L2/3 | 12,483 | 12,506 | 99.82% | 445,823 |
| L5 | 7,378 | 7,378 | 100.00% | 930,512 |
| L6 | 9,004 | 9,004 | 100.00% | 774,017 |
| prefrontal | 0 | 0 | 0.00% | 0 |

总计 35,702 / 35,726 个活跃神经元达到当前 chi² 显著性阈值。

这表明 L4–L6 已经形成很强的输入选择性，但前额叶层完全没有参与。由于同时对大量神经元进行显著性检验，最终研究报告还应加入多重比较校正；不过当前显著比例接近 100%，传统校正预计不会改变“低层选择性很强”的总体判断。

最终字节响应累计量：

| 类别 | 响应计数 | 比例 |
|---|---:|---:|
| ASCII `<0x80` | 314,710,777 | 25.6% |
| UTF-8 头字节 | 318,823,446 | 26.0% |
| UTF-8 续字节 | 593,758,735 | 48.4% |

网络对中文 UTF-8 输入形成了稳定响应，但这仍然属于字节层表征，不能等价为中文语义理解。

## 7. 权重与可塑性结果

### 7.1 权重分布

| 指标 | 数值 |
|---|---:|
| 全局均值 | 0.3340 |
| 标准差 | 0.5918 |
| 中位数 | 0.7500 |
| 弱突触 `|w|<0.1` | 15.09% |
| 强突触 `|w|>1.0` | 0.01% |
| 抑制性突触 | 20.00% |
| 兴奋性突触 | 80.00% |
| `[0.5,1.0)` | 62.07% |
| `[-1.0,-0.5)` | 19.14% |

大量权重集中在约 `+0.75/-0.75` 附近，强突触比例仅 0.01%。这与 PSW 成熟率 0% 一致，说明大部分突触没有形成广泛、成熟的概率权重证据。

### 7.2 生物学习机制

| 机制 | 最终值 | 状态 |
|---|---:|---|
| CaMKII 非零 | 544/100,000 | PASS，但非常稀疏 |
| CaMKII mean | 0.000017 | 活性弱 |
| eligibility e1 | 0 | FAIL |
| eligibility e2 | 0 | FAIL |
| PSW evidence mean | 0.0630 | 低 |
| PSW confidence mean | 0.0593 | 低 |
| PSW mature ratio | 0% | FAIL |
| Ca²⁺ mean | 0.000653 | 低 |
| Ca²⁺ max | 0.1200 | 未达到 0.15 回弹阈值 |
| DA | 0.159 | 合法 |
| ACh | 0.554 | 合法 |
| NE | 0.103 | 合法 |
| 5-HT | 0.353 | 合法 |
| V(s) | 0.0716 | 非零 |
| V(s′) | 0.0131 | 非零 |

神经调质和价值函数处于合法范围，但 eligibility 完全为零，使奖励/调质难以通过时间信用分配真正影响突触。CaMKII 虽然非零，但覆盖率和均值都很低。

日志中 Ca²⁺ 回弹判据显示 PASS，实际含义是状态合法且没有越界，不代表回弹 LTD 已经触发；当前最大 Ca²⁺ 为 0.12，仍低于 0.15 的触发阈值。

## 8. P3、工作记忆与门控

| 指标 | 最终值 | 状态 |
|---|---:|---|
| P3 inhibitory updates | 300,000 | 有调度 |
| P3 activity drive | 0.0000 | FAIL |
| k-WTA updates | 300,000 | PASS |
| active columns | 11/50 | 部分活跃 |
| winner estimate | 31 | 有竞争结果 |
| suppressed estimate | 1,195 | 抑制有效 |
| 工作记忆 updates | 30,000 | 有调度 |
| 工作记忆 active slots | 0/50 | FAIL |
| 工作记忆 mean/max | 0/0 | FAIL |
| prefrontal active | 0 | FAIL |
| semantic readiness | 0.55 | 低于 0.70 |
| gate mean | 0.6007 | PASS |
| gate open ratio | 1.0000 | PASS，但始终开启 |

k-WTA 的局部竞争机制可以运行，但其上游 activity drive 为零，工作记忆和前额叶没有激活。门控始终开启也意味着当前门控没有表现出明显的状态选择性。

这条链路是本轮训练的主要瓶颈：

```text
eligibility = 0
    ↓
时间信用分配不足
    ↓
P3 activity drive = 0
    ↓
工作记忆槽 = 0
    ↓
前额叶活动 = 0
    ↓
上下文语义组合尚未形成
```

## 9. 最终科学判据

### 9.1 明确通过

- 编译与 3M 步无崩溃；
- 显存预算；
- 脉冲动态范围和 burst；
- 延迟事件链路；
- 权重范围；
- CaMKII 非零；
- 神经调质范围；
- 字节直方图方差；
- DA 价值函数；
- 分层字节选择性；
- k-WTA；
- P3-C 语义聚类；
- 丘脑门控运行。

### 9.2 明确未通过

- PSW 概率权重成熟；
- eligibility trace；
- P3 activity drive；
- 工作记忆槽激活；
- 语义准备度 0.70；
- 前额叶有效活动。

### 9.3 统计实现导致的假失败

- 平均脉冲判据因 32 位累计计数溢出显示为负数；修正后约为 1,057.41 spikes/step，应视为通过。

最终日志状态为：

```text
RUN_STATUS: COMPLETE
科学判据未全部成熟
```

## 10. 能力边界

| 能力 | 当前证据 | 结论 |
|---|---|---|
| 大规模 SNN 长程稳定运行 | 3M 步、零丢事件、断点校验 | 强 |
| 中文 UTF-8 字节响应 | 155 个非零字节 bins、分层高选择性 | 强 |
| 输入选择性 | 35,702 个显著神经元 | 强 |
| 稀疏柱竞争 | k-WTA 有效，11/50 柱活跃 | 中等 |
| 无监督聚类 | silhouette 约 0.20–0.225 | 中等 |
| 时间关联学习 | eligibility 为零 | 未形成 |
| PSW 成熟学习 | mature ratio 0% | 未形成 |
| 工作记忆 | 0/50 | 未形成 |
| 前额叶上下文表征 | 无活动 | 未形成 |
| 文本预测 | 未进行固定留出集评测 | 未证明 |
| 文本生成 | 无解码/生成评测 | 未证明 |

## 11. 文本预测与文本生成功能现状

### 11.1 当前没有真正的文本输出功能

截至本次 3M 训练完成，Stage 2e 只实现了文本输入、网络活动统计和内部发放率预测，尚未实现从脉冲活动到字节或 token 的输出解码器。

| 功能 | 当前状态 | 说明 |
|---|---|---|
| 加载 UTF-8 文本 | 已实现 | 从 LCCC 文件读取原始字节流 |
| 群体编码注入 | 已实现 | 每个字节映射到 L4 群体输入 |
| 字节响应统计 | 已实现 | 统计神经元、层和柱对输入字节的响应 |
| 200维发放率预测 | 部分实现 | `w_pred` 预测下一时刻亚柱发放率 |
| 下一字节概率 | 未实现 | 没有 `P(byte_next | context)` 输出 |
| 下一 token 概率 | 未实现 | 当前系统没有 tokenizer 和 token 输出头 |
| 固定留出集预测评测 | 未实现 | 没有 top-1、top-k、交叉熵或困惑度 |
| Prompt 推理接口 | 未实现 | CLI 没有 prompt/inference/generate 模式 |
| 自回归文本生成 | 未实现 | 没有采样、回注和停止条件 |

### 11.2 “注入文本还原”不是模型生成

早期报告中的以下文本：

```text
我饿了。去相机家里吃……相机今年木有回去……
```

来自程序对“实际注入的前 64 字节”进行 UTF-8 解码和打印。源码明确写明：

```cpp
// 通过注入位置追踪还原 (简化: 仅显示实际注入的前 64 字节)
```

参见 [`main.cpp`](../src/stage2e/main.cpp#L1225)。这段输出只能证明输入文件被正确读取和注入，不能证明网络从脉冲中重建或生成了文本。

### 11.3 `w_pred` 不是 next-byte 预测器

断点中已经保存：

- `w_pred`：200×200；
- `subcolumn_fr`：200维当前亚柱发放率；
- `pred_fr`：200维预测发放率。

但当前实现仅更新 `w_pred` 的对角项：

```cpp
w_pred[k * dim + k] += ETA_PRED * pred_error * subcolumn_fr[k];
pred_fr[k] = w_pred[k * dim + k] * subcolumn_fr[k];
```

参见 [`modulatory_kernels.cu`](../src/stage2e/modulatory_kernels.cu#L123)。它学习的是：

```text
当前亚柱发放状态 → 下一时刻亚柱发放状态
```

它没有将预测发放映射到 256 个字节，也没有产生 softmax 概率，因此不能直接输出下一个字节。

项目 README 同样把 next-byte/next-token 解码器列为尚未完成的 Phase R5，参见 [`README.md`](../README.md#L91)。

### 11.4 不重训 Stage 2e 的预测验证方案

首先应增加独立、只读的 next-byte probe。该实验不重新训练 55K 神经元和 10.7M 突触，也不修改 3M 断点：

1. 加载 `ckpt_step3000000.snn2e`；
2. 冻结 STDP、PSW、eligibility、结构可塑性和全部 SNN 权重；
3. 对未参与 Stage 2e 训练的连续语料执行前向传播；
4. 从 L4、L2/3、L5、L6、P3 柱和前额叶采集窗口脉冲特征；
5. 仅训练一个外部线性读出，将神经活动映射为 256 个下一字节分数；
6. 在独立测试段上评测，并与 unigram 和 byte-bigram 基线比较。

外部线性读出属于诊断探针，不修改 Stage 2e 核心网络。它回答的问题是：

> 3M 断点中是否已经存在可被简单模型读取的下一字节信息？

建议采用连续的 train/validation/test 时间切分，禁止随机打散相邻字节，避免未来信息泄漏。

### 11.5 文本预测验收指标

| 指标 | 作用 |
|---|---|
| Unigram top-1 | 只按训练段最高频字节预测的最低基线 |
| Byte-bigram top-1 | 只知道当前字节时的强基线 |
| Probe top-1 | SNN 活动预测下一字节的准确率 |
| Probe top-5 | 下一字节是否落在前5个候选中 |
| Cross-entropy | 概率分布质量 |
| Bits per byte | 字节级语言建模能力 |
| Context ablation | 打乱历史活动后性能下降多少 |
| Shuffled-text control | 保留字节频率、破坏上下文后的性能 |

只有当 SNN probe 在独立测试段上稳定超过 byte-bigram，并且打乱历史活动后性能显著下降，才能认为网络中存在超出局部字节频率的上下文预测信息。

### 11.6 自回归文本生成路线

如果 next-byte probe 达标，可在保持 Stage 2e 核心拓扑不变的前提下增加外部输出读出和自回归控制循环：

```text
Prompt UTF-8 字节
        ↓
Stage 2e 冻结网络前向运行
        ↓
读取当前脉冲/亚柱状态
        ↓
256维下一字节 logits
        ↓
softmax + temperature/top-k 采样
        ↓
预测字节回注 Stage 2e
        ↓
重复，形成字节流
```

生成系统还必须处理：

- UTF-8 非法字节序列；
- Prompt 与生成阶段的状态初始化；
- temperature、top-k/top-p；
- 最大生成长度和停止条件；
- 重复循环检测；
- 固定随机种子下的可复现生成；
- 与 unigram/bigram 生成基线的盲评。

增加外部读出不会改变 Stage 2e 的 55K/10.7M 核心拓扑，但严格来说会为完整系统增加一个输出头。若要求任何参数结构都不能增加，则只能做只读 probe，无法形成可观察的文本生成接口。

### 11.7 基于当前 3M 指标的预期

当前 silhouette 和字节选择性说明 3M 断点可能包含可解码的输入身份信息，因此 next-byte probe 值得进行。但以下结果限制了生成预期：

- eligibility trace 为零；
- 工作记忆槽 0/50；
- P3 activity drive 为零；
- 前额叶无活动；
- JS mean 低于目标；
- silhouette 在 10K–3M 基本没有净增长。

因此，在没有探针数据前，合理预期是模型可能达到字节频率或短程 bigram 水平，但尚没有证据支持长上下文、连贯中文生成。不能把解码器能够输出字符等价为 SNN 已经获得语言能力。

## 12. 方法学风险与谬误扫描

覆盖：11/11。

| 类型 | 严重度 | 本实验情况 |
|---|---|---|
| Simpson’s paradox | NOTE | 没有按语料类别、字符类别或随机种子分层，暂时无法检查趋势反转 |
| Ecological fallacy | CAUTION | 不能从柱/神经元聚类直接推断整体语言理解 |
| Berkson’s paradox | NOTE | 当前不是筛选人群数据，主要不适用 |
| Collider bias | NOTE | 没有回归控制变量模型，主要不适用 |
| Base-rate neglect | CAUTION | 字节频率高度不均衡，强响应可能部分来自基础频率 |
| Regression to mean | NOTE | 没有依据极端指标筛选训练样本，主要不适用 |
| Survivorship bias | CAUTION | 只报告当前成功长程运行，缺少全部试运行和失败运行清单 |
| Look-elsewhere effect | CAUTION | 同时观察大量层、神经元和指标，需控制多重比较并预先定义主指标 |
| Garden of forking paths | CAUTION | 评估器、阈值和诊断过程经过迭代，缺少预注册的固定分析方案 |
| Correlation ≠ causation | CAUTION | 指标随训练变化不能单独证明由特定学习规则造成 |
| Reverse causality | CAUTION | 没有关闭单个机制的消融实验，无法确认指标变化的因果方向 |

其他关键限制：

- 仅一个正式随机种子；
- 没有独立重复训练；
- 没有固定留出语料；
- k-means 单次初始化会产生明显异常；
- chi² 和 col_ratio 未按固定窗口归一化；
- 没有下一字节准确率、交叉熵、困惑度或生成质量指标；
- 10K/20K/100K 与 3M 的部分统计口径并不完全一致。

## 13. 下一阶段建议

下一阶段应保持 Stage 2e 架构不变，不新增层、不减少神经元、不改变 55K/10.7M 拓扑规模。

### P0：先修复评估可信度

1. 将累计脉冲、burst 和其他长程计数器改为 `uint64_t`。
2. silhouette 使用固定随机种子并进行至少 20–30 次 k-means restart，报告均值、标准差和最佳值。
3. 使用固定留出语料和固定长度验证窗口。
4. chi² 按注入次数归一化，并进行多重比较校正。
5. 明确区分累计统计、窗口统计和瞬时统计。

### P1：做架构不变的短程因果探针

从 3M 断点分别进行 50K–100K 的短实验：

1. 检查 eligibility 写入前后的门控条件、衰减和清零路径；
2. 记录 eligibility 激活率，而不仅是最终非零数；
3. 检查 P3 activity drive 的输入值、阈值和抑制路径；
4. 检查工作记忆槽接收的实际电流和门控；
5. 检查前额叶连接是否存在事件到达但膜电位未越阈；
6. 每次只调整一个已有参数，保留严格对照。

### P2：建立真正的语言能力对照

至少加入以下对照：

- 原始 LCCC 文本；
- 打乱字节顺序的 LCCC；
- 保持字节频率但破坏上下文的随机语料；
- 单字节循环；
- 未参与训练的 LCCC 留出片段。

评测指标应增加：

- 下一字节 top-1/top-k 准确率；
- 交叉熵或可比较的预测损失；
- 上下文长度变化下的预测增益；
- 表征线性探针；
- 可重复的固定标签聚类指标；
- 若已有解码器，再评估生成连贯性。

## 14. 最终结论

Stage 2e 3M 训练是一次成功的大规模 SNN 工程验证，也是一次部分成功的表征学习实验：

- 网络没有崩溃或沉默；
- 低层和中层形成了强烈、稳定的中文字节选择性；
- silhouette 指标稳定高于 0.15；
- 但大部分语义聚类在 10K 左右已经出现，3M 没有带来明显净提升；
- eligibility、PSW、工作记忆和前额叶链路没有成熟；
- 当前结果不能支持“已经学会中文”或“已经成为语言模型”的结论。

最值得保留的成果是：**在完全保留 Stage 2e 架构的情况下，55K 神经元、10.7M 突触系统已经证明可以稳定训练到 3M，并形成可测量的输入选择性和初步聚类。**

下一次突破点不是继续无条件堆训练步数，而是修通现有架构中的时间信用分配和工作记忆通路，并建立固定、可复现的语言能力评测。
