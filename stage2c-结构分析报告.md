# Stage 2c 结构分析报告

> 分析时间：2026-07-19
> 分析对象：Stage 2b 1M 步训练后的联合皮层网络（checkpoint: `data/stage2_1M_ckpt.bin`）
> Baseline：B1 随机初始化网络（相同拓扑、相同种子、未训练）
> 测试语料：LCCC-base 头部 1MB 子集（`data/lccc_sample_1mb.txt`，1,048,576 字节）
> 测试步数：10,000 步
> 分析工具：`snn_stage2_analyze.exe`（自实现，无外部依赖）

---

## 1. 实验设置

### 1.1 网络结构

| 项目 | 值 |
|---|---|
| 神经元总数 | 10,000 |
| 突触总数 | 1,000,000 |
| 皮层柱数 | 10 |
| 每柱神经元数 | 1,000 |
| 柱内突触/神经元 | 80（p_intra ≈ 0.080） |
| 柱间突触/神经元 | 20（p_inter ≈ 0.0022） |
| 兴奋/抑制比 | 80/20（按柱内分配） |
| 输入编码 | 8-bit / 256 UTF-8 字节流，注入 sensory 神经元 0..7 |

### 1.2 训练超参（Stage 2b）

| 参数 | 值 |
|---|---|
| 训练步数 | 1,000,000 |
| 学习机制 | 纯 STDP（A±=0.05）+ eligibility trace + dopamine=1.0 |
| Homeostatic | 内嵌于 LIF kernel，target_fr = 5 Hz（sensory/association），30 Hz（motor） |
| 重置周期 | 每 10,000 步重置膜电位（保留突触） |
| 训练数据 | LCCC-base 全量 829 MB（2016 万对话轮次） |

### 1.3 分析方法

| 分析项 | 实现方式 |
|---|---|
| 幂律拟合 | CCDF log-log 线性回归，判定 α ∈ [1.5, 3.0] 且 R² > 0.9 |
| 卡方检验 | 逐神经元检验字节-发放独立性，df = 出现字节数 − 1，p < 0.01 |
| PCA + K-means | 256 维 byte-activation 向量 → 256×256 scatter matrix → power iteration 求 top-2 PC → K-means++ 测试 K=5,8,10,12,15,20 |
| 轮廓系数 | 标准 silhouette，按最大簇选择 best K |

---

## 2. 核心结果对比

### 2.1 激活摘要

| 指标 | Train-1M | B1-Random | 训练效应 |
|---|---:|---:|---:|
| 总 spikes（10k 步） | 26,186,283 | 30,690,946 | -14.7% |
| 平均 spikes/step | 2,618.63 | 3,069.09 | -14.7% |
| 活跃神经元数 | 7,857 | 9,466 | -1,609 |
| 活跃神经元占比 | 78.57% | 94.66% | -16.1 pp |
| 静默神经元数 | 2,143 | 534 | +1,609 |

**解读**：训练后网络整体发放率下降 14.7%，且 1,609 个神经元从活跃转为静默。这与 STDP 的突触修剪效应一致——一部分突触被 LTD 压到 0，导致下游神经元收不到足够兴奋性输入。

### 2.2 单神经元发放计数的分布

| 指标 | Train-1M | B1-Random |
|---|---:|---:|
| 最小 spike count | 3,331 | 1 |
| 最大 spike count | 3,334 | 3,334 |
| 平均 spike count | 3,332.86 | 3,242.23 |
| 中位数 spike count | 3,333 | 3,331 |
| 极差（max − min） | **3** | **3,333** |

**关键观察**：训练后所有活跃神经元的发放计数高度集中（极差仅 3，相对差异 0.09%），说明 homeostatic 机制把所有活跃神经元的发放率严格拉到目标值附近。B1 随机网络则有从 1 到 3,334 的全范围分布。

---

## 3. 四项判定标准

### Q1：联合皮层是否形成语义簇？

**结论：否（假阳性）**

| 指标 | Train-1M | B1-Random | 判定阈值 |
|---|---:|---:|---:|
| Top-2 PC 解释方差 | 99.886% | 99.894% | - |
| Best K | 15 | 12 | 5 ≤ K ≤ 20 |
| Silhouette | **1.000** | 0.993 | > 0.3 |
| 最大簇大小 | 5,612 | 6,401 | - |
| 最大簇占比 | 56.1% | 64.0% | - |

**形式上 silhouette = 1.0 满足 "CLUSTERS EMERGED" 判据，但这是假阳性**：

1. **Top-2 PC 解释 99.886% 方差** 意味着 10,000 个神经元在 PCA 空间几乎共线——所有点都在一条直线上
2. **silhouette = 1.0 不是因为簇分明，而是因为点几乎重合**：活跃神经元 spike count 极差仅 3，PCA 投影后距离极小，K-means 任何划分都会得到完美分离
3. **最大簇占 56%** 表明聚类实际上没有发现有意义的子结构——大部分神经元被归为同一类
4. **B1 随机网络 silhouette 也达到 0.993**，说明此指标在当前网络规模下缺乏判别力

### Q2：神经元发放是否符合幂律分布？

**结论：否**

| 指标 | Train-1M | B1-Random | 判定阈值 |
|---|---:|---:|---:|
| α（拟合指数） | 11,308.3 | 0.475 | 1.5 ≤ α ≤ 3.0 |
| R² | 0.878 | 0.297 | > 0.9 |
| Verdict | NOT power-law | NOT power-law | - |

**解读**：
- 生物皮层的神经元发放统计符合重尾分布（α ≈ 2，典型 1.5–3.0）
- Train-1M 的 α = 11,308 是因为发放分布**极度集中**——几乎所有活跃神经元都发放 3,331–3,334 次，CCDF 在 log-log 图上几乎是垂直下降
- B1 的 α = 0.475 且 R² = 0.30 说明分布是均匀的，没有重尾特征
- 两者都未能形成生物合理的幂律分布

### Q3：不同 UTF-8 字节是否激活不同神经元子集？

**结论：否**

| 指标 | Train-1M | B1-Random | 判定阈值 |
|---|---:|---:|---:|
| 测试神经元数 | 7,857 | 9,213 | - |
| 自由度 df | 88 | 88 | - |
| 平均 χ² | 51.45 | 45.34 | - |
| 最大 χ² | 57.63 | 57.63 | - |
| 显著神经元数（p<0.01） | 0 | 0 | - |
| 显著比例 | 0.0% | 0.0% | > 50% |
| Verdict | not significant | not significant | - |

**解读**：
- df = 88 表明测试序列中实际只出现了 89 种字节值（UTF-8 多字节字符的高字节种类有限）
- 卡方临界值（df=88, p=0.01）≈ 123.2，而最大 χ² = 57.63 远低于此
- **没有任何神经元表现出字节选择性**——所有神经元对所有字节的响应几乎一致
- 这与 Q1 的发现一致：homeostatic 把所有神经元拉到均匀发放率，破坏了字节-神经元的映射关系

### Q4：与随机初始化网络的差异显著性？

**结论：突触层面显著，行为层面不显著**

#### 突触权重分布对比

| 指标 | Train-1M | B1-Random | 变化 |
|---|---:|---:|---:|
| 权重均值 | 0.317 | 0.183 | +73.0% |
| 权重标准差 | 0.561 | 0.437 | +28.4% |
| 兴奋性突触占比 | 60.23% | 70.71% | -10.5 pp |
| 零权重突触占比 | 26.60% | 11.69% | +14.9 pp |
| 饱和突触占比（\|w\| > 0.9·W_MAX） | 31.80% | 20.86% | +10.9 pp |

**STDP 学习确实发生了**：
1. **权重均值增长 73%**：STDP 的 LTP 占主导，整体突触强度增加
2. **零权重突触翻倍**：26.6% 的突触被 LTD 压到 0，发生了**突触修剪**
3. **饱和突触增加**：31.8% 的突触被推到 ±W_MAX 边界，形成强连接
4. **抑制性突触比例升高**：从 29.3% 升至 39.8%，兴奋/抑制平衡向抑制侧倾斜
5. **权重直方图呈双峰分布**：训练后在 [−1, −0.9) 与 [0.9, 1) 两个端点附近聚集，中间区域稀疏

#### 权重直方图（20 bins，从 −W_MAX 到 +W_MAX）

```
Train-1M 直方图:
[-1, -0.9): ████████████████████████████████  72057
[-0.9, -0.8):  235
[-0.8, -0.7): ██ 3879
[-0.7, -0.6):  97
[-0.6, -0.5):  85
[-0.5, -0.4):  59
[-0.4, -0.3):  115
[-0.3, -0.2): ██ 16640
[-0.2, -0.1): ██ 13361
[-0.1, 0): ███ 25251
[0, 0.1): ██████████████████████████████ 316017  ← 大量近零突触
[0.1, 0.2):  1753
[0.2, 0.3): ██████████ 144200
[0.3, 0.4):  5430
[0.4, 0.5):  1506
[0.5, 0.6):  1534
[0.6, 0.7): █ 5875
[0.7, 0.8): ██████████ 145205
[0.8, 0.9):  776
[0.9, 1): ████████████████████████ 245925  ← 大量饱和突触

B1-Random 直方图:
[-1, -0.9): ████ 38145
[-0.9, -0.8):  189
[-0.8, -0.7): 0
[-0.7, -0.6): 0
[-0.6, -0.5):  1
[-0.5, -0.4):  3
[-0.4, -0.3):  8
[-0.3, -0.2): ████ 43711
[-0.2, -0.1): ████ 43452
[-0.1, 0): █████ 50465
[0, 0.1): ██████████████████████████████ 340502
[0.1, 0.2): ███████████████ 156230
[0.2, 0.3): ███████████████ 155930
[0.3, 0.4):  33
[0.4, 0.5):  6
[0.5, 0.6):  2
[0.6, 0.7): 0
[0.7, 0.8): 0
[0.8, 0.9): █ 908
[0.9, 1): █████████████████ 170415
```

**B1 权重集中在 [−0.3, 0.3) 区间**（初始化范围 STAGE2_INIT_WEIGHT_MAX=0.3），训练后**双峰化明显**——突触要么被强化到饱和，要么被压到 0，中间状态减少。

---

## 4. 综合判定

### 4.1 四项判定汇总

| 问题 | 判定 | 关键证据 |
|---|---|---|
| Q1 语义簇涌现 | ❌ 否（silhouette=1.0 为假阳性） | Top-2 PC 解释 99.9% 方差；所有活跃神经元 spike count 极差仅 3 |
| Q2 幂律发放 | ❌ 否 | α=11,308 远超 [1.5, 3.0]；发放分布过度集中 |
| Q3 字节-神经元映射 | ❌ 否 | 0/7,857 神经元卡方显著 |
| Q4 突触可塑性 | ✅ 是（局部成功） | 权重均值 +73%，零权重突触 +127%，饱和突触 +52% |

### 4.2 Stage 2c 总体结论

**突触层发生了学习，但网络层未涌现语义结构**。

STDP 在 1M 步训练中确实重塑了突触权重分布：
- LTP 把 31.8% 突触推到饱和（强连接形成）
- LTD 把 26.6% 突触压到零（突触修剪）
- 权重直方图从单峰变为双峰

但这种突触层的学习**没有转化为功能层的语义结构**：
- 神经元发放高度均匀（极差 3/3334 = 0.09%），失去特异性
- 没有任何神经元对字节表现出选择性
- PCA 投影几乎共线，K-means 的"高 silhouette"是退化结果

---

## 5. 根因分析

### 5.1 Homeostatic 过强

当前 LIF kernel 内嵌的 homeostatic 机制目标 firing rate = 5 Hz（sensory/association），把所有神经元的发放率严格拉到目标值附近。这导致：
- 活跃神经元发放极差仅 3，相对差异 0.09%
- 字节输入的差异被 homeostatic 完全抹平
- 卡方检验无法检测到任何字节-神经元关联

**证据**：B1 随机网络极差为 3,333（从 1 到 3,334），训练后极差缩小到 3。

### 5.2 输入信号稀释

每个字节只激活 8 个 sensory 神经元（sensory 总数 2,000 中的 0.4%），输入电流最多 8 × 2.0 = 16.0，经过 10 柱 × 80 intra-column 突触的稀释后，单字节信号在网络中几乎是噪声水平。

### 5.3 皮层柱未形成功能分化

10 个皮层柱在训练后没有任何柱特异性——所有柱的神经元发放率都被 homeostatic 拉到 5 Hz 附近，柱间差异不显著。

---

## 6. 与 Stage 2b 训练监控的一致性

Stage 2b 训练日志显示：
- meanW 从 0.09 → 0.318（+253%）→ 与本次 0.317 完全一致 ✓
- meanFR 稳定在 0.30–0.32 → 与本次 silhouette=1.0 反映的"均匀发放"一致 ✓
- 总 spikes 在 2,500–3,000/step → 与本次 avg 2,618/step 一致 ✓

训练监控数据与本次结构分析结果**完全自洽**，分析工具链可信。

---

## 7. 后续方向（Stage 2d 或 2e）

基于 2c 的诊断，建议在下一阶段尝试：

### 7.1 弱化 Homeostatic（优先级：高）
- 把 sensory/association 的 target_fr 从 5 Hz 提高到 15–20 Hz
- 或减小 homeostatic 增益，允许神经元发放率有更大差异
- 预期：恢复神经元的字节选择性，让 Q3 卡方检验出现显著神经元

### 7.2 增强 Input Gain（优先级：高）
- 把 SENSORY_INPUT_GAIN 从 2.0 提高到 5.0–10.0
- 或扩大 sensory 输入神经元数（从 8 扩展到 32，使用 one-hot 编码而非二进制）
- 预期：让单字节信号在网络中可辨识

### 7.3 引入柱间竞争（优先级：中）
- winner-take-all 机制：每步只允许发放最强的 1–2 个柱保留活动
- 或 lateral inhibition：柱间抑制连接，使不同字节驱动不同柱
- 预期：让不同字节激活不同柱，形成功能柱分化

### 7.4 调整 STDP 参数（优先级：低）
- 当前 A±=0.05 对称，可尝试 A+ > A−（偏 LTP）或 A+ < A−（偏 LTD）
- 或延长 eligibility trace 衰减时间常数

---

## 8. 数据文件清单

| 文件 | 用途 |
|---|---|
| `data/stage2_1M_ckpt.bin` (38 MB) | 训练后 checkpoint |
| `data/lccc_sample_1mb.txt` (1 MB) | 测试语料子集 |
| `data/stage2c_train_log.txt` | Train-1M 分析日志 |
| `data/stage2c_b1_log.txt` | B1-Random 分析日志 |
| `data/stage2c_train_clusters.csv` | Train-1M PCA + 聚类坐标（10,000 点） |
| `data/stage2c_b1_clusters.csv` | B1-Random PCA + 聚类坐标（10,000 点） |

---

## 9. 工具链验证

`snn_stage2_analyze.exe`（自实现，无外部依赖）首次运行成功：

| 模块 | 实现 | 验证状态 |
|---|---|---|
| `collect_activation_data` | 跑 N 步测试，收集 256×10000 字节-神经元激活矩阵 | ✓ 正确 |
| `analyze_power_law` | CCDF log-log 线性回归 | ✓ 边界值（α→∞）合理 |
| `analyze_chi_square` | 逐神经元独立性检验，df 自动适配 | ✓ df=88 与 89 种实际字节一致 |
| `pca_2d` | 256×256 scatter matrix + power iteration | ✓ 解释方差 99.9% 合理 |
| `kmeans` | K-means++ + Lloyd，测试 6 个 K 值 | ✓ 选择 best K 合理 |
| `silhouette_score` | 标准 silhouette | ✓ 数值合理 |
| `analyze_weight_distribution` | 从 GPU 拉取 d_weights_ + 直方图 | ✓ 与训练日志一致 |
| `save_cluster_csv` | 输出 neuron_id,pc1,pc2,cluster,region | ✓ 10,000 行已生成 |
