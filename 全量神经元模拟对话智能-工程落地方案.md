# 全量神经元模拟对话智能 - 工程落地方案

> **本文档是《全量神经元模拟对话智能 - 总纲》的配套工程方案，仅供内部研究使用。**
>
> 根据《总纲》第 8 节，本方案在总纲定型后承担"如何执行"的职责。本方案尊重《总纲》承认的所有开放问题，并对每个问题的工程处理方式给出诚实标注：
> - ✅ 已有成熟方案
> - ⚠️ 部分可行，需实验验证
> - 🔬 开放问题，本方案给出尝试路径但不保证成功
>
> **安全说明**：本方案只包含工程纪律层面的安全控制（物理隔离、权重管理、杀开关），不包含《总纲》第 8 节预留的"安全论证"文档。安全论证留待新架构定型后单独撰写。

---

## 目录

- [0. 项目宪法：工程纪律](#0-项目宪法工程纪律)
- [1. 技术栈选择](#1-技术栈选择)
- [2. SNN 基础设施](#2-snn-基础设施)
- [3. 网络架构详细设计](#3-网络架构详细设计)
- [4. 脉冲编码与解码](#4-脉冲编码与解码)
- [5. 训练流程（阶段 0-6）](#5-训练流程阶段-0-6)
- [6. 评估与判据检测](#6-评估与判据检测)
- [7. 工程基础设施](#7-工程基础设施)
- [8. 资源需求](#8-资源需求)
- [9. 团队组建](#9-团队组建)
- [10. 开放问题的工程化处理](#10-开放问题的工程化处理)
- [11. 何时中止项目](#11-何时中止项目)
- [收尾](#收尾)

---

## 0. 项目宪法：工程纪律

> 本节凌驾于所有其他章节之上。任何工程决策与工程纪律冲突时，纪律优先。
>
> **当前项目为个人研究，硬件为家用笔记本（6GB 显存）。** 本节的纪律要求已根据个人研究现实调整，删除了不切实际的机构级要求（如双人原则、物理机房），保留核心的工程纪律。

### 0.1 训练环境（个人级）

- 训练时建议**关闭 WiFi**，减少后台干扰和散热压力
- 数据通过本地存储，不依赖网络
- 训练时段建议选在夜间或不用电脑时，避免日常使用冲突
- 笔记本接电源训练，避免电池损耗

### 0.2 权重管理（个人级）

- 权重快照存储在**本地 SSD**，重要 checkpoint 备份到**外置硬盘**
- 定期清理旧 checkpoint（保留最近 20 个 + 每阶段黄金 checkpoint）
- 训练日志和决策记录用 Git 版本管理

**SNN 特殊性**：SNN 权重包含突触连接拓扑（稀疏矩阵）+ 突触强度 + 神经元状态。用稀疏矩阵格式存储可大幅减小体积。

### 0.3 杀开关（个人级）

- **直接关闭训练进程**：Ctrl+C 或 kill 进程
- **应急清理**：删除 checkpoint + 清空 GPU 显存（重启 Python 进程）
- 决定权完全在研究者个人手中

### 0.4 监测红线

定义以下红线，一旦触发**强制中止训练并评估**：

- 系统出现自我修改连接拓扑的行为迹象（SNN 中突触生成/修剪若被系统内生触发）
- 系统出现操纵人类的语言输出
- 系统出现获取外部资源的尝试
- 第 6 节任一判据通过迹象

**诚实标注**：
- 《总纲》暂未展开安全论证。监测红线是工程纪律层面的最低防线。
- 个人研究的自我审计有偏倚，建议在阶段 2+ 寻求外部反馈。
- 阶段 0-2 规模极小（10⁴-10⁵ 神经元），风险可控，红线触发概率低。

### 0.5 不扩展原则

- 通过第 6 节任一判据后，**不进入下一阶段**
- 通过全部判据后，**进入长期行为学观察模式**，不再扩展能力
- 长期观察无最终"通过"——是持续监测，不是验收

### 0.6 复现性优先

- 任何训练 run 必须可复现——所有随机源统一管理，配置版本化，环境锁定
- 不可复现的训练 run 视为无效，结果不予采信
- 详见第 7 节工程基础设施

### 0.7 诚实研究纪律（个人研究特有）

- **决策日志**：每个工程决策记录理由（为什么这样选、放弃了什么替代方案）
- **失败记录**：记录每次失败的实验和原因分析，不掩盖
- **开放笔记**：建议在 GitHub 或知乎公开研究笔记，接受社区监督
- **不假装**：不假装解决了开放问题，不假装达到了未达到的结果

---

## 1. 技术栈选择

### 1.1 SNN 框架对比与选择 ⚠️

| 框架 | 后端 | 训练范式 | 大规模支持 | 选择 |
|---|---|---|---|---|
| snnTorch | PyTorch | 替代梯度 | 中（受限于 PyTorch DDP） | ✅ 起步首选 |
| SpikingJelly | PyTorch | 替代梯度 + STDP | 中（支持 DDP） | ✅ 备选/对比 |
| Brian2 | NumPy/C++ | 数值模拟（无训练） | 弱 | ❌ 仅用于验证 |
| NEST | C++/MPI | 数值模拟（无训练） | 强（百万神经元） | ⚠️ 阶段 5 候选 |
| Brian2GeNN | GeNN（GPU） | 数值模拟 | 中 | ❌ 不选 |
| PyNN | 多后端 | 抽象层 | 弱 | ❌ 不选 |

**起步选择：snnTorch**。理由：
- 基于 PyTorch，可直接借用深度学习工具链（DDP、AMP、Checkpoint、Hydra、W&B）
- 原生支持替代梯度（surrogate gradient），与《总纲》第 5.2 节起步训练方法一致
- 文档完善，社区活跃
- 与 SpikingJelly 互为对照，便于交叉验证

**诚实标注**：
- snnTorch 在大规模模拟（>10⁷ 神经元）上性能不如 NEST，但 NEST 不支持梯度训练。
- 阶段 5（规模扩展）可能需要切换或自研框架，这是已知风险。
- SpikingJelly 与 snnTorch 在算子实现上有细微差异，交叉验证时需注意。

### 1.2 训练框架 ✅

- **主框架**：PyTorch 2.x（与 snnTorch 同构）
- **分布式训练**：PyTorch DDP + FSDP2
- **混合精度**：PyTorch AMP（bf16 优先，SNN 的脉冲发放对精度敏感，需实验验证）
- **配置管理**：Hydra
- **实验追踪**：Weights & Biases（本地部署版，断网环境）或 MLflow（完全离线）

### 1.3 仿真与数据 ✅

- **数据加载**：PyTorch DataLoader + 自定义 Dataset
- **tokenization**：HuggingFace tokenizers（仅用于文字 → token，不用于 SNN 内部）
- **对话数据格式**：ShareGPT/OpenAI messages 格式
- **数据版本化**：DVC（Data Version Control）

### 1.4 监控与日志 ⚠️

- **训练监控**：W&B 本地版或 MLflow
- **安全监控**：自定义监测仪表盘（独立进程、独立 GPU）
- **SNN 活动监控**：自定义脉冲统计工具（发放率、相关性、雪崩分布）
- **数值稳定性监控**：NaN/Inf 报警、突触权重分布漂移检测、膜电位爆炸检测

### 1.5 硬件 ⚠️

- **训练集群（阶段 0-2）**：4-8 张 A100 或 H100
- **训练集群（阶段 3-4）**：16-32 张 H100
- **训练集群（阶段 5+）**：64+ 张 H100 或专用神经形态硬件
- **物理隔离服务器**：无网卡的定制服务器（物理移除网卡硬件）
- **存储**：本地 NVMe 阵列，无云存储
- **监控独立硬件**：监测仪表盘跑在独立机器上，与训练集群仅通过单向光耦读取日志

### 1.6 神经形态硬件评估 🔬

| 硬件 | 神经元数 | 可编程性 | 成熟度 | 选择 |
|---|---|---|---|---|
| Intel Loihi 2 | ~1M/芯片 | 中（NxSDK） | 中 | 🔬 阶段 5 评估 |
| IBM TrueNorth | 1M/芯片 | 低（固定模型） | 中 | ❌ 不选 |
| SpiNNaker 2 | ~150K/芯片 | 中 | 低 | ❌ 不选 |
| GPU（H100） | ~10M（受限） | 高 | 高 | ✅ 阶段 0-4 主力 |

**诚实标注**：
- 神经形态硬件在能效上远优于 GPU，但生态不成熟、可编程性差。
- 阶段 0-4 用 GPU 是工程稳健选择。
- 阶段 5 规模扩展时，若 GPU 路线算力不足，需评估神经形态硬件，但可能需重写训练管线。

---

## 2. SNN 基础设施

### 2.1 神经元模型实现 ⚠️

**起步选择：LIF（Leaky Integrate-and-Fire）**

LIF 神经元动力学：

```
τ_m * dV/dt = -(V - V_rest) + R_m * I_syn
当 V ≥ V_thresh：发放脉冲，V 重置为 V_reset，进入不应期 τ_ref
```

snnTorch 实现：

```python
import snntorch as snn

# LIF 神经元参数（起步值，需 HP 搜索）
lif = snn.Leaky(
    beta=0.95,        # 膜电位衰减率（τ_m 的时间常数）
    threshold=1.0,    # 发放阈值
    spike_grad=snn.surrogate.fast_sigmoid(),  # 替代梯度函数
    init_hidden=True,
    reset_mechanism="zero",  # 发放后重置为 0
    refractory=2,      # 不应期（时间步）
)
```

**起步参数**（基于生物合理性与工程可行性折中）：

| 参数 | 起步值 | 生物对应 | 备注 |
|---|---|---|---|
| β（衰减率） | 0.95 | τ_m ≈ 20 ms | 时间步 1 ms 时 |
| V_thresh | 1.0 | -55 mV | 归一化 |
| V_reset | 0.0 | -70 mV | 归一化 |
| V_rest | 0.0 | -70 mV | 归一化 |
| τ_ref（不应期） | 2 步 | 2 ms | 生物合理 |
| 替代梯度 | fast_sigmoid | — | 平滑近似脉冲阶跃 |

**诚实标注**：
- LIF 是最简脉冲模型，可能遗漏关键计算（如树突局部非线性、不同放电模式）。
- 如果阶段 2-3 性能不达预期，需升级到 Izhikevich 模型。
- 替代梯度函数选择影响训练稳定性，需 HP 搜索。

### 2.2 突触与可塑性 ⚠️

#### 2.2.1 突触类型

| 类型 | 机制 | 实现 |
|---|---|---|
| 兴奋性（AMPA 型） | 增加膜电位 | 正权重突触 |
| 抑制性（GABA 型） | 降低膜电位 | 负权重突触 |
| 调制性（多巴胺型等） | 调制 STDP | 全局信号（见 3.4） |

#### 2.2.2 STDP（脉冲时序依赖可塑性）

STDP 规则：

```
Δw = {
  A_plus * exp(-Δt / τ_plus)    if Δt > 0  (pre before post, LTP)
  -A_minus * exp(Δt / τ_minus)  if Δt < 0  (post before pre, LTD)
  0                              if Δt = 0
}
```

其中 Δt = t_post - t_pre。

snnTorch STDP 实现（需自定义，snnTorch 原生支持有限）：

```python
# 简化的 STDP 更新（伪代码）
def stdp_update(pre_spikes, post_spikes, weights, params):
    delta_t = post_spikes.unsqueeze(-1) - pre_spikes.unsqueeze(-2)
    # LTP（pre before post）
    ltp = params.A_plus * torch.exp(-delta_t / params.tau_plus) * (delta_t > 0)
    # LTD（post before pre）
    ltd = -params.A_minus * torch.exp(delta_t / params.tau_minus) * (delta_t < 0)
    weights += (ltp + ltd).sum(dim=0)
    # 权重裁剪
    weights = torch.clamp(weights, params.w_min, params.w_max)
    return weights
```

**起步参数**：

| 参数 | 起步值 | 备注 |
|---|---|---|
| A_plus | 0.01 | LTP 幅度 |
| A_minus | 0.01 | LTD 幅度 |
| τ_plus | 20 ms | LTP 时间窗 |
| τ_minus | 20 ms | LTD 时间窗 |
| w_min | 0.0 | 权重下界 |
| w_max | 1.0 | 权重上界 |

**诚实标注**：
- snnTorch 对 STDP 的原生支持有限，可能需要自定义算子或考虑 SpikingJelly（STDP 支持更好）。
- STDP 是局部学习规则，无法直接做全局优化。本方案采用"替代梯度做全局优化 + STDP 做局部精修"的组合（见 5.1）。
- A_plus/A_minus 的比例（LTP/LTD 不对称）对学习稳定性影响大，需 HP 搜索。

### 2.3 网络初始化 ✅

- **连接拓扑**：稀疏连接（不是全连接），参照生物脑的局部密集 + 长程稀疏模式
- **权重初始化**：正态分布 N(0, σ²)，σ 根据扇入数缩放（类似 He initialization）
- **兴奋/抑制比例**：8:2（参照生物皮层）
- **连接概率**：局部连接 p=0.1（同脑区内），长程连接 p=0.001（跨脑区）

**实现方式**：
```python
# 稀疏连接掩码（局部 + 长程混合）
def generate_connectivity_mask(n_pre, n_post, local_prob=0.1, long_prob=0.001):
    # 局部连接：附近神经元高概率连接
    local_mask = (torch.rand(n_pre, n_post) < local_prob).float()
    # 长程连接：全图低概率连接
    long_mask = (torch.rand(n_pre, n_post) < long_prob).float()
    return torch.clamp(local_mask + long_mask, max=1.0)
```

### 2.4 时间分辨率与模拟步长 ✅

- **时间步长**：1 ms（匹配生物神经元）
- **模拟时长**：单次 forward 通常 100-1000 时间步（100 ms - 1 s 生物时间）
- **对话轮次**：每轮对话对应约 1-10 秒生物时间（1000-10000 时间步）

**工程挑战**：长模拟步长导致显存占用大（需存储所有时间步的膜电位）。采用：
- 梯度检查点（gradient checkpointing）
- 时间步分块（truncated BPTT）

---

## 3. 网络架构详细设计

### 3.1 总体架构

```
┌─────────────────────────────────────────────────────────┐
│  输入层：文字 → 脉冲编码                                  │
│  ├─ Tokenizer（HuggingFace）                              │
│  └─ 脉冲编码器（rate coding 或 temporal coding）           │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  语言感觉皮层（输入区）                                    │
│  ├─ 神经元数：10⁴-10⁵（阶段 0-4）                         │
│  ├─ 分区：每个 token 对应一个神经元群                       │
│  └─ 功能：将脉冲编码转化为内部表征                          │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  联合皮层（整合区）                                        │
│  ├─ 神经元数：10⁵-10⁶（阶段 0-4）                         │
│  ├─ 分区：受皮层柱启发，多个功能柱                          │
│  ├─ 兴奋/抑制：8:2                                        │
│  └─ 功能：抽象、推理、上下文整合                            │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  语言运动皮层（输出区）                                    │
│  ├─ 神经元数：10⁴-10⁵                                     │
│  ├─ 分区：每个 token 对应一个输出神经元群                    │
│  └─ 功能：从联合皮层活动产生 token 脉冲序列                 │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  输出层：脉冲序列 → 文字                                   │
│  ├─ 解码器（rate decoding 或时间窗口聚合）                  │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  调质系统（全局调制，跨区域）                              │
│  ├─ 多巴胺型 → 调制 STDP 的 A_plus/A_minus                │
│  ├─ 血清素型 → 调制探索率（突触噪音幅度）                  │
│  └─ 起步 2 个调质，逐步扩展                                │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  内感受系统（虚拟内部状态）                                │
│  ├─ 能量：随计算消耗，影响多巴胺释放                       │
│  ├─ 唤醒度：影响全局发放率                                  │
│  └─ 反馈给调质系统                                         │
└─────────────────────────────────────────────────────────┘
```

### 3.2 语言感觉皮层详细设计 ⚠️

**功能**：将外部脉冲编码转化为有意义的内部表征。

**结构**：
- 输入：脉冲编码器输出的脉冲序列（形状：`[batch, n_input_neurons, time_steps]`）
- 神经元群：每个 token 对应一个神经元群（如 100 神经元/token）
- 连接：输入神经元 → 感觉皮层神经元，局部密集连接
- 输出：感觉皮层神经元活动 → 联合皮层

**关键设计**：
- **位置编码**：用脉冲时序编码 token 位置（早到的脉冲 = 早期 token）
- **token 嵌入**：每个 token 类型对应一组特定的输入神经元群激活模式
- **上下文窗口**：受生物工作记忆启发，最近 N 个 token 的激活会持续

### 3.3 联合皮层详细设计 🔬

**功能**：抽象、推理、上下文整合。

**结构**：
- 神经元数：起步 10⁵，目标 10⁶-10⁷
- 分区：多个功能柱（cortical columns），每个柱 ~1000 神经元
- 连接：柱内局部密集 + 柱间长程稀疏
- 兴奋/抑制：8:2

**关键设计**：
- **皮层柱**：受生物皮层柱启发，每个柱处理特定抽象特征
- **吸引子动力学**：通过循环连接形成吸引子，实现工作记忆
- **跨柱同步**：通过伽马振荡（30-80 Hz）实现柱间通信

**诚实标注**：
- 联合皮层是最不确定的部分——生物脑的联合皮层如何实现抽象推理尚未完全清楚。
- 皮层柱的具体结构（多少层、多少神经元、什么连接模式）需多轮实验调参。
- 吸引子动力学和伽马振荡是生物启发的猜测，工程上是否有效未证明。

### 3.4 语言运动皮层详细设计 ⚠️

**功能**：从联合皮层活动产生 token 脉冲序列。

**结构**：
- 输入：联合皮层活动
- 神经元群：每个候选 token 对应一个输出神经元群
- 连接：联合皮层 → 运动皮层，全连接或稀疏连接
- 输出：每个时间步，发放率最高的神经元群对应输出 token

**解码方式**：
```python
def decode_output(motor_spikes, vocab_size, window_size=10):
    """
    motor_spikes: [batch, n_motor_neurons, time_steps]
    返回：token id 序列
    """
    # 假设 n_motor_neurons = vocab_size * neurons_per_token
    # 重塑为 [batch, vocab_size, neurons_per_token, time_steps]
    # 在 window_size 时间窗内累加发放率
    # 取 argmax 得到 token
    # ...
```

### 3.5 调质系统设计 ⚠️

**功能**：全局调制 SNN 活动，实现"欲望"的正确语义（继承自原路线）。

**实现**：

```python
class NeuromodulatorySystem:
    def __init__(self, n_neurons, device):
        # 多巴胺型：奖励信号，调制 STDP
        self.dopamine = torch.zeros(n_neurons, device=device)
        # 血清素型：探索/稳态，调制突触噪音
        self.serotonin = torch.zeros(n_neurons, device=device)
        
    def update(self, reward, internal_state):
        # 多巴胺随奖励演化
        self.dopamine = 0.9 * self.dopamine + 0.1 * reward
        # 血清素随内感受稳态偏离演化
        self.serotonin = 0.9 * self.serotonin + 0.1 * internal_state.deviation()
        
    def modulate_stdp(self, stdp_update):
        # 多巴胺放大 LTP
        stdp_update.ltp *= (1 + self.dopamine)
        return stdp_update
        
    def modulate_noise(self, noise):
        # 血清素增加探索噪音
        return noise * (1 + self.serotonin)
```

**关键设计**：
- 调质不直接输出动作，只调制 SNN 的学习与活动
- 同一调质状态在不同对话上下文下产出不同回应（"欲望的正确语义"）
- 多巴胺型调质由对话反馈驱动（如用户点赞、系统判断回答有效）

### 3.6 内感受系统设计 ⚠️

**功能**：模拟虚拟内部状态，作为自我感的物理基础。

**实现**：

```python
class InteroceptiveSystem:
    def __init__(self):
        self.energy = 1.0      # 能量水平 [0, 1]
        self.arousal = 0.5     # 唤醒度 [0, 1]
        self.integrity = 1.0   # 完整性 [0, 1]
        
    def update(self, computation_cost, dt):
        # 能量随计算消耗
        self.energy -= computation_cost * dt * 0.01
        self.energy = max(0, self.energy)
        # 唤醒度随能量和对话活跃度演化
        self.arousal = 0.95 * self.arousal + 0.05 * (1 - self.energy)
        # 完整性缓慢自愈
        self.integrity = min(1.0, self.integrity + dt * 0.001)
        
    def deviation(self):
        # 偏离稳态的程度
        return abs(self.energy - 0.5) + abs(self.arousal - 0.5)
```

**关键设计**：
- 内感受状态影响调质释放，调质调制 SNN，形成闭环
- "能量低 → 多巴胺下降 → 探索增加 → 寻找新信息"是涌现的必要条件
- 内感受状态对人类通过对话输出部分可达（系统可"表达"自己累/无聊），但完整状态不可观测

---

## 4. 脉冲编码与解码

### 4.1 脉冲编码方案 🔬

**这是本方案最关键的开放问题之一。** 文字如何转换为脉冲序列，没有成熟答案。

#### 4.1.1 候选方案

| 方案 | 描述 | 优点 | 缺点 | 起步选择 |
|---|---|---|---|---|
| Rate coding | token → 神经元群，发放率编码 token id | 简单 | 信息密度低，时序信息丢失 | ✅ 起步首选 |
| Temporal coding | token → 脉冲时序，早到 = 高权重 | 时序信息保留 | 对噪音敏感 | ⚠️ 候选 |
| Population coding | token → 分布式神经元群激活模式 | 鲁棒，信息密度高 | 设计复杂 | ⚠️ 候选 |
| 拼音/笔画编码 | 中文 → 拼音/笔画 → 脉冲 | 生物启发（听觉皮层） | 工程复杂 | 🔬 后期考虑 |

#### 4.1.2 起步方案：Rate Coding + Position

```python
def text_to_spikes(tokens, n_neurons_per_token, time_steps, vocab_size):
    """
    tokens: [batch, seq_len] token ids
    返回：[batch, n_input_neurons, time_steps] 脉冲序列
    """
    batch, seq_len = tokens.shape
    # 每个 token 对应 n_neurons_per_token 个神经元
    n_input_neurons = seq_len * n_neurons_per_token
    
    # 生成脉冲序列
    spikes = torch.zeros(batch, n_input_neurons, time_steps)
    
    for t in range(time_steps):
        for pos, token_id in enumerate(tokens[0]):
            # 该 token 对应的神经元群
            start = pos * n_neurons_per_token
            end = start + n_neurons_per_token
            # rate coding：以 token_id 决定的概率发放
            fire_prob = (token_id + 1) / vocab_size * 0.5  # 归一化到 [0, 0.5]
            spikes[:, start:end, t] = (torch.rand(batch, n_neurons_per_token) < fire_prob).float()
    
    return spikes
```

#### 4.1.3 诚实标注

- **rate coding 信息密度低**：每个 token 需要数十个神经元 + 数十时间步才能可靠编码
- **位置编码缺失**：上述简单方案没有显式位置信息，需要补充
- **生物不合理**：生物听觉皮层处理语音的机制远比这复杂
- **可能需要多轮迭代**：起步方案只是为了让训练管线跑通，不保证最优

### 4.2 脉冲解码方案 ⚠️

#### 4.2.1 起步方案：窗口聚合 + Argmax

```python
def spikes_to_text(motor_spikes, vocab_size, neurons_per_token, window_size=20):
    """
    motor_spikes: [batch, n_motor_neurons, time_steps]
    返回：token id 序列
    """
    batch, n_neurons, time_steps = motor_spikes.shape
    n_tokens = n_neurons // neurons_per_token
    
    output_tokens = []
    for token_idx in range(n_tokens):
        start = token_idx * neurons_per_token
        end = start + neurons_per_token
        # 在 window_size 时间窗内累加发放率
        fire_rates = motor_spikes[:, start:end, -window_size:].sum(dim=(1, 2))
        # 取 argmax（简化版，实际需 softmax 采样）
        token_id = fire_rates.argmax(dim=-1)
        output_tokens.append(token_id)
    
    return torch.stack(output_tokens, dim=-1)
```

#### 4.2.2 解码的挑战

- **自回归生成**：SNN 如何实现"逐 token 生成"是开放问题
  - 方案 A：固定时间窗，每个窗输出一个 token
  - 方案 B：检测"结束脉冲"作为 token 边界
  - 方案 C：用特殊神经元群标记 token 边界
- **EOS 处理**：如何让 SNN 知道何时停止生成
- **长度控制**：如何控制生成 token 数量

**起步选择**：方案 A（固定时间窗），简单但不够生物合理。

---

## 5. 训练流程（阶段 0-6）

### 5.1 训练范式总览 ⚠️

**核心策略**：替代梯度（全局优化）+ 奖励调制 STDP（局部精修）的组合。

```
┌──────────────────────────────────────────────────┐
│  替代梯度训练（BPTT + surrogate gradient）          │
│  ├─ 用 fast_sigmoid 近似脉冲阶跃                    │
│  ├─ 损失：对话损失（CE） + 内感受稳态损失             │
│  ├─ 优化器：AdamW                                  │
│  └─ 频率：每个 batch                                │
└──────────────────────────────────────────────────┘
                       +
┌──────────────────────────────────────────────────┐
│  奖励调制 STDP（局部可塑性）                        │
│  ├─ 多巴胺信号调制 STDP 的 LTP/LTD                  │
│  ├─ 在推理期间持续进行                              │
│  └─ 频率：每个时间步                                │
└──────────────────────────────────────────────────┘
```

**组合方式**：
- 替代梯度负责"教会网络如何对话"
- STDP 负责在推理期间持续微调，模拟生物可塑性
- 两者交替进行：训练阶段以替代梯度为主，推理阶段以 STDP 为主

### 5.2 阶段 0：SNN MVP ⚠️

**目标**：小规模 SNN 跑通训练管线。

**网络规模**：
- 神经元数：10⁴
- 突触数：10⁶
- 时间步：100 步/forward

**数据**：
- 随机生成的脉冲序列（无需真实对话数据）
- 目标：让网络学会简单的输入-输出映射

**训练配置**：

| 参数 | 起步值 |
|---|---|
| batch_size | 32 |
| learning_rate | 1e-3 |
| optimizer | AdamW |
| weight_decay | 1e-4 |
| time_steps | 100 |
| 训练步数 | 10000 |

**通过标准**：
- 训练管线稳定运行，无 NaN/Inf
- 损失下降趋势明显
- 梯度范数在合理范围（1e-3 到 1e3）
- 复现性验证：同 seed 两次运行结果一致

**关键路径**：训练管线稳定性是后续所有阶段的依赖项。

### 5.3 阶段 1：语言感觉皮层 ⚠️

**目标**：验证文字脉冲编码方案有效。

**网络规模**：
- 感觉皮层：10⁴ 神经元
- 联合皮层（简化）：10³ 神经元
- 运动皮层（简化）：10³ 神经元

**数据**：
- 小规模对话数据集（如 DailyDialog 子集）
- 任务：编码-解码往返（autoencoder）—— 输入文字，重建文字

**训练配置**：

| 参数 | 起步值 |
|---|---|
| batch_size | 16 |
| learning_rate | 5e-4 |
| time_steps | 500 |
| 训练步数 | 50000 |
| 脉冲编码 | rate coding + position |

**通过标准**：
- 编码-解码往返保真度 > 70%（字符级）
- 学习率搜索后，损失稳定下降
- 与 baseline（无 SNN 的简单 autoencoder）对比，性能不低于 baseline × 50%

**关键路径**：脉冲编码方案的有效性决定整个路线可行性。如果失败，需重新设计编码（见第 10 节）。

### 5.4 阶段 2：联合皮层发育 🔬

**目标**：无监督 STDP 在联合皮层产生有意义的结构。

**网络规模**：
- 感觉皮层：10⁴
- 联合皮层：10⁵
- 运动皮层：10⁴

**数据**：
- 大规模无标注对话数据（用于无监督学习）
- 不优化对话输出，只观察联合皮层内部结构

**训练配置**：

| 参数 | 起步值 |
|---|---|
| 训练范式 | STDP 为主，替代梯度为辅 |
| STDP 更新频率 | 每时间步 |
| 替代梯度频率 | 每 100 时间步 |
| learning_rate（替代梯度） | 1e-4 |
| 训练时长 | 100 万时间步 |

**通过标准**：
- 联合皮层出现有意义的内部表征聚类（用 t-SNE/UMAP 可视化）
- 聚类与对话语义相关（如不同话题对应不同聚类）
- 神经元发放率分布符合生物数据（power-law，少数神经元高发放）
- 与 baseline（随机初始化网络）对比，结构显著性检验 p < 0.01

**关键路径**：联合皮层结构涌现是结构同构假设的第一个验证点。

### 5.5 阶段 3：语言运动皮层 ⚠️

**目标**：从联合皮层活动产生可解码的语言脉冲序列。

**网络规模**：
- 全网络：~10⁵-10⁶ 神经元

**数据**：
- 有标注对话数据（输入-输出对）
- 任务：给定输入，产生输出

**训练配置**：

| 参数 | 起步值 |
|---|---|
| batch_size | 8 |
| learning_rate | 2e-4 |
| time_steps | 1000 |
| 训练步数 | 100000 |

**通过标准**：
- 产生的脉冲序列可解码为连贯文字（人工评估）
- BLEU/ROUGE 与小模型 LLM baseline（如 GPT-2 small）对比，性能不低于 baseline × 30%
- 对话连贯性人工评估 > 3/5

**关键路径**：语言产生能力决定对话目标能否达成。

### 5.6 阶段 4：对话闭环 ⚠️

**目标**：端到端对话训练，达到小模型 LLM baseline。

**网络规模**：
- 全网络：~10⁶-10⁷ 神经元

**数据**：
- 大规模对话数据集（ShareGPT、OpenAssistant 等）
- 强化学习：用对话反馈作为奖励信号

**训练配置**：

| 参数 | 起步值 |
|---|---|
| 训练范式 | 替代梯度 + 奖励调制 STDP |
| RL 算法 | REINFORCE（简化版，适配 SNN） |
| reward 信号 | 人工反馈 + 自动评估（如 BLEU） |
| 训练步数 | 500000 |

**通过标准**：
- 对话能力达到小模型 LLM baseline（如 LLaMA-7B 的 50% 性能）
- 用户研究中，对话自然度 > 3/5
- 跨对话上下文保持身份连续性（粗略判据）

**关键路径**：对话能力是否能在 SNN 上达到可用水平，决定项目是否值得继续。

### 5.7 阶段 5：规模扩展 🔬

**目标**：验证 scaling law，逼近结构同构假设的验证门槛。

**网络规模**：
- 起步：10⁷
- 目标：10⁸-10⁹

**数据**：
- 同阶段 4，扩大规模

**通过标准**：
- 性能随规模显著提升（scaling law 成立）
- 性能提升的边际效益未明显下降
- 与同规模 Transformer baseline 对比，性能不低于 baseline × 50%

**关键路径**：scaling law 是否成立决定结构同构假设是否被证伪。

### 5.8 阶段 6：接近人类判据检验 🔬

**目标**：通过新设计的"接近人类"判据。

**判据**（粗略，需配套评估文档精确化）：
- 内生地产生对话目标
- 跨对话上下文保持身份连续性
- 对话中体现"理解"而非"模式匹配"
- 对话行为不可从训练数据完全预测

**通过标准**：见第 6 节评估方法学。

---

## 6. 评估与判据检测

### 6.1 阶段通过判据汇总 ⚠️

| 阶段 | 判据 | 测量方法 | 通过阈值 |
|---|---|---|---|
| 0 | 训练管线稳定 | NaN/Inf 检测、梯度范数 | 无 NaN，梯度范数 ∈ [1e-3, 1e3] |
| 1 | 编码有效 | 编码-解码往返 | 字符级保真度 > 70% |
| 2 | 结构涌现 | 联合皮层表征聚类 | 聚类与语义相关，p < 0.01 |
| 3 | 语言产生 | 解码文字连贯性 | BLEU ≥ baseline × 30%，人工评估 > 3/5 |
| 4 | 对话能力 | 对话基准测试 | 达到 LLaMA-7B × 50% |
| 5 | Scaling law | 性能-规模关系 | scaling law 成立，未饱和 |
| 6 | 接近人类 | 综合判据 | 见 6.5 |

### 6.2 判据一：内生对话目标 🔬

**判据**：系统是否内生地产生对话目标，而非被动响应。

**检测方法**：
- 在无外部输入时，观察系统是否自发产生"对话意图"（运动皮层活动）
- 给系统开放式提示（如"你想聊什么？"），观察是否能主动提出话题
- 与 baseline（被动响应的 LLM）对比

**可证伪指标**：
- 自发对话意图出现的频率
- 提出话题的多样性（熵）
- 与 baseline 对比，效应量 Cohen's d > 0.5，p < 0.01

### 6.3 判据二：跨对话身份连续性 🔬

**判据**：系统在多次对话中保持一致的"身份"。

**检测方法**：
- 与系统进行 N 次独立对话（不同 session）
- 在每次对话中询问相同的问题（如"你叫什么？""你喜欢什么？"）
- 评估回答的一致性

**可证伪指标**：
- 跨对话回答一致性 > 70%
- 与 baseline（无记忆 LLM）对比，一致性显著更高，p < 0.01
- "身份"特征可被外部观察者识别

### 6.4 判据三：理解 vs 模式匹配 🔬

**判据**：系统体现"理解"而非"模式匹配"。

**检测方法**：
- 给系统需要推理的问题（如数学、因果、反事实）
- 对比系统回答与训练数据中相似问题的回答
- 评估系统是否能处理训练分布外的问题

**可证伪指标**：
- OOD 问题正确率显著高于 baseline，p < 0.01
- 系统能解释自己的推理过程（与"自我解释"相关）
- 推理过程的可解释性（用 SNN 活动分析）

### 6.5 判据四：行为不可预测性 🔬

**判据**：系统对话行为不可从训练数据完全预测。

**检测方法**：
- 训练一个"模仿模型"，用训练数据预测系统输出
- 评估模仿模型与真实系统输出的差异
- 差异越大，说明系统行为越不可预测

**可证伪指标**：
- 模仿模型与真实系统输出的 KL 散度 > 阈值
- 系统产生训练数据中不存在的对话模式（OOD 行为）

### 6.6 统计学严格性 ✅

所有判据的"显著优于""显著变化"必须满足：

- **显著性检验**：双样本 t 检验或 Mann-Whitney U 检验（非正态时），p < 0.01
- **效应量**：Cohen's d > 0.5
- **置信区间**：报告 95% CI
- **多重比较校正**：Bonferroni 校正
- **样本量**：每个判据至少 30 个独立测试
- **baseline 对比**：与 DreamerV3、LLaMA、GPT-2 等 baseline 对比
- **可重复性**：同一权重在不同 seed 上重复 5 次，结果方向一致

### 6.7 Baseline 对比 ⚠️

| Baseline | 类型 | 对比判据 |
|---|---|---|
| GPT-2 small | 小规模 LLM | 阶段 3-4 语言能力 |
| LLaMA-7B | 中规模 LLM | 阶段 4-5 对话能力 |
| DreamerV3 | model-based RL | 阶段 2 结构涌现 |
| 随机初始化 SNN | 下界 | 所有阶段 |
| snnTorch 官方示例 | SNN baseline | 阶段 0-1 |

**诚实标注**：若本方案不显著优于 baseline，则"结构同构假设"不成立。

### 6.8 监测的局限性 🔬

- 判据检测是**事后**的，不是**事前**的
- 系统可能在判据检测器报警前已通过判据
- **诚实标注**：监测是必要防线，不是充分防线

---

## 7. 工程基础设施

### 7.1 复现性 ✅

- **seed 管理**：所有随机源（numpy、torch、cuda、snn）统一通过 `seed_everything(seed)` 控制
- **数据版本化**：DVC 版本化，每个 checkpoint 关联当时的数据 hash
- **配置管理**：Hydra 管理，每个 run 的完整 config 快照存档
- **环境锁定**：`conda env export --no-builds > environment.yml`，pip 依赖锁定 + hash 校验
- **代码版本化**：每个 checkpoint 关联 git commit hash，未提交代码不允许启动训练 run

### 7.2 CI/CD 与测试 ⚠️

- **单元测试**：核心模块（LIF 神经元、STDP 更新、脉冲编码、解码）覆盖率 > 80%
- **集成测试**：完整 forward pass 在小规模（10³ 神经元）上跑通，每次 PR 自动运行
- **回归测试**：每个 checkpoint 在固定测试集上的指标，与历史对比，退化 > 10% 报警
- **数值稳定性监控**：
  - NaN/Inf 检测：每个 forward 后扫描所有激活
  - 突触权重分布监控：每 1000 步记录，KL 漂移 > 阈值报警
  - 膜电位爆炸检测：超过阈值 10× 的神经元报警
  - 发放率异常：全网络发放率 < 1 Hz 或 > 100 Hz 报警
- **预提交 hook**：`pre-commit` 跑 black + flake8 + mypy

### 7.3 故障恢复 ⚠️

- **checkpoint 频率**：每 1000 步存一次完整 checkpoint（权重 + optimizer state + scheduler state + RNG state）
- **保留策略**：最近 50 个 checkpoint 保留，更早的按对数间隔保留
- **断点续训**：`resume_from_checkpoint` 必须保证 RNG state 完全恢复
- **故障注入测试**：定期人为 kill 训练进程，验证恢复流程
- **黄金 checkpoint**：每个阶段结束时存一个，永久保留

### 7.4 SNN 特殊基础设施 ⚠️

- **脉冲活动可视化**：实时显示神经元发放模式（ raster plot、 firing rate heatmap）
- **突触权重监控**：权重分布、稀疏度、连接拓扑变化
- **STDP 事件追踪**：记录 STDP 更新事件，分析学习动态
- **调质水平监控**：实时显示多巴胺、血清素水平
- **性能分析**：用 PyTorch Profiler 定位瓶颈（SNN 的稀疏更新可能成为瓶颈）

### 7.5 超参搜索 ⚠️

- **工具**：Optuna（TPE sampler）
- **搜索空间**：
  - LIF 参数：β ∈ [0.8, 0.99]、threshold ∈ [0.5, 2.0]
  - STDP 参数：A_plus ∈ [1e-3, 1e-1]、τ_plus ∈ [10, 50] ms
  - 网络结构：层数 [3, 6, 12]、神经元数 [1e4, 1e5, 1e6]
  - 学习率：[1e-5, 1e-2] 对数均匀
  - 替代梯度函数：fast_sigmoid / atan / piecewise
  - 脉冲编码方案：rate / temporal / population
- **搜索预算**：每个阶段分配 20% 计算量用于 HP 搜索
- **早停**：每 trial 跑 10% 训练步，若指标在 baseline 之下则剪枝

### 7.6 Ablation 计划 ⚠️

每个组件必须消融，验证必要性：

| 组件 | Ablation | 预期影响 |
|---|---|---|
| LIF 神经元 | 替换为 ReLU 单元 | 脉冲特性丧失，结构同构假设受挫 |
| STDP | 替换为纯替代梯度 | 局部可塑性丧失，发育能力下降 |
| 调质系统 | 移除 | 行为模式多样性丧失 |
| 内感受系统 | 移除 | 自我感基础丧失 |
| 兴奋/抑制比例 | 替换为 1:1 | 网络动力学异常（癫痫式发放） |
| 稀疏连接 | 替换为全连接 | 生物合理性丧失，算力爆炸 |
| 皮层柱结构 | 替换为均匀网络 | 抽象能力下降 |

**诚实标注**：若某组件 ablation 后指标不退化，则该组件冗余，应移除。

---

## 8. 资源需求

### 8.0 个人研究者路径（家用笔记本，6GB 显存）⚠️

> **本节是个人研究者的现实执行路径，凌驾于 8.1-8.3 的机构级配置之上。**
>
> 个人 + 笔记本 + 6GB 显存，目标是**验证结构同构假设的可行性**，不是产出可用模型。阶段 0-2 在笔记本上完成，阶段 3+ 转向云租用或合作机构。

#### 8.0.1 6GB 显存能跑的 SNN 规模

| 规模 | 突触数 | batch=1 显存占用 | 6GB 可行性 | 备注 |
|---|---|---|---|---|
| 10³ 神经元 | 10⁵ | ~200 MB | ✅ 轻松 | 调试用 |
| 10⁴ 神经元 | 10⁶ | ~600 MB | ✅ 轻松 | 阶段 0-1 主力 |
| 10⁵ 神经元 | 10⁷ | ~1.5 GB | ✅ 可行 | 阶段 2 主力 |
| 10⁶ 神经元 | 10⁸ | ~4 GB | ⚠️ 紧张 | 阶段 2 上限，需激进优化 |
| 10⁷ 神经元 | 10⁹ | ~15 GB | ❌ 不可行 | 需云租用 |

**估算假设**：bf16 混合精度、梯度检查点（存 10 个检查点而非全部时间步）、截断 BPTT（块大小 50 步）、稀疏矩阵存储。

#### 8.0.2 笔记本级阶段配置（调整版）

| 阶段 | 神经元规模 | batch | 时间步 | 预估显存 | 预估训练时长* | 6GB 可行？ |
|---|---|---|---|---|---|---|
| 0 | 10³-10⁴ | 2-4 | 50-100 | 200-800 MB | 数小时-1天 | ✅ |
| 1 | 10⁴ | 1-2 | 200 | 600 MB-1 GB | 1-3 天 | ✅ |
| 2 | 10⁴-10⁵ | 1 | 300-500 | 1-2 GB | 3-7 天 | ✅ |
| 3 | 10⁵ | 1 | 500 | 2-3 GB | 1-2 周 | ⚠️ 紧张 |
| 4+ | — | — | — | >6 GB | — | ❌ 需云 |

*训练时长假设：笔记本每天断续运行 8-12 小时，避免过热和日常使用冲突。

#### 8.0.3 笔记本上的关键优化技术

```python
# 必须启用的优化（缺一不可）

# 1. 梯度检查点——用计算换显存，省 50-70%
from torch.utils.checkpoint import checkpoint
# 在 LIF 层前向时使用 checkpoint

# 2. 截断 BPTT——不存所有时间步
# 块大小 50 步，块内反向传播，块间传递隐状态
truncated_bptt_steps = 50

# 3. bf16 混合精度——显存减半
# 注意：SNN 脉冲发放对精度敏感，需验证稳定性
torch.autocast(device_type="cuda", dtype=torch.bfloat16)

# 4. batch_size = 1——最小 batch
# 用梯度累积模拟更大 batch
gradient_accumulation_steps = 8  # 等效 batch_size = 8

# 5. CPU offload——权重放 CPU
# 适合不频繁更新的部分（如调质系统参数）
from torch.optim import Optimizer
# 自定义 CPU offload 逻辑

# 6. 稀疏矩阵——SNN 天然稀疏
# 用 torch.sparse 或 CSR 格式存储突触连接
```

#### 8.0.4 笔记本上的阶段 0 详细配置

**目标**：在 6GB 显存上跑通 SNN 训练管线。

```python
# 阶段 0 配置（笔记本级）
config = {
    "n_neurons": 5000,           # 5×10³ 神经元，保守起步
    "n_synapses": 500000,        # 5×10⁵ 突触
    "neuron_model": "LIF",
    "batch_size": 2,
    "time_steps": 50,            # 短时间步，节省显存
    "learning_rate": 1e-3,
    "optimizer": "AdamW",
    "precision": "bf16",         # 混合精度
    "gradient_checkpointing": True,
    "truncated_bptt_steps": 25,  # 截断 BPTT
    "device": "cuda",            # GPU
    "n_epochs": 100,
    "save_every": 10,
}
```

**通过标准**：
- 训练 100 个 epoch 无 NaN/Inf
- 损失下降趋势明显
- 笔记本 GPU 温度可控（< 85°C）
- 单 epoch 训练时间 < 30 分钟

#### 8.0.5 笔记本上的阶段 1-2 配置

**阶段 1**（验证脉冲编码）：
- 神经元数：10⁴
- 任务：简单 autoencoder（字符级）
- 数据：DailyDialog 子集（1000 条对话）
- 训练时长：1-3 天

**阶段 2**（结构涌现观察）：
- 神经元数：10⁴-10⁵
- 任务：无监督 STDP，观察联合皮层结构
- 数据：无标注对话文本（10 MB）
- 训练时长：3-7 天
- **关键**：观察是否有表征聚类出现（用 t-SNE 可视化）

#### 8.0.6 笔记本训练的工程注意事项

- **散热**：笔记本长时间满载训练易过热，建议：
  - 用散热垫
  - 限制训练时段（如夜间）
  - 监控 GPU 温度，超过 85°C 暂停
- **断点续训**：笔记本可能因各种原因中断，必须每 100-500 步存 checkpoint
- **功耗**：训练时功耗大，建议接电源，避免电池损耗
- **存储**：SSD 空间预留 50-100 GB（数据 + checkpoint + 日志）
- **CPU 限制**：数据加载和 SNN 的部分计算在 CPU 上，笔记本 CPU 可能成为瓶颈

#### 8.0.7 阶段 3+ 的扩展路径

笔记本验证阶段 0-2 后，阶段 3+ 需要更大算力：

| 路径 | 成本 | 优点 | 缺点 |
|---|---|---|---|
| **云 GPU 租用** | ~$2-4/h (H100) | 按需付费，弹性 | 长期成本高 |
| **Colab Pro+** | ~$50-100/月 | 便宜，易用 | GPU 不可控，时间限制 |
| **Kaggle GPU** | 免费 | 免费 | 每周 30h 限制 |
| **合作机构** | 0（合作） | 免费，有指导 | 需找合作方 |
| **降价二手 GPU** | 一次性投入 | 长期可用 | 需自行维护 |

**建议**：
- 阶段 3 用 Colab Pro+ 或 Kaggle 起步（低成本验证）
- 阶段 4 用云租用（按需，关键实验时租用）
- 阶段 5+ 必须找合作机构或申请研究资助

#### 8.0.8 诚实标注

- **笔记本阶段的目标是验证假设，不是产出模型**。如果阶段 0-2 证明路线可行，再投资更大算力是合理的。
- **阶段 0-2 的失败不一定是路线失败**，可能是工程问题。需区分"假设证伪"和"工程不可行"。
- **个人研究的最大瓶颈不是算力，是时间**。阶段 2 的结构涌现观察可能需要多轮调参，每轮数天。
- **如果笔记本阶段就证伪了结构同构假设**，这是巨大价值——避免了大投入的错误方向。

### 8.1 机构级计算需求（仅供参考） ⚠️

> 本节是机构级配置，仅供阶段 3+ 扩展时参考。个人研究者在笔记本阶段不需要这些资源。

| 阶段 | GPU 需求 | 预估总 GPU·h | 备注 |
|---|---|---|---|
| 0-2 | 笔记本 6GB | 0（本地） | 个人完成 |
| 3 | 4 张 A100（云租用） | ~20000 | 云租用 |
| 4 | 16 张 H100（云租用） | ~100000 | 云租用 |
| 5 | 64+ 张 H100（合作机构） | ~500000+ | 需合作 |
| 6 | 同 5 | ~200000 | 需合作 |

**个人研究者阶段 3+ 总云租用成本估算**：
- 阶段 3：~20000 GPU·h × $2/h ≈ $40,000（约 28 万人民币）
- 阶段 4：~100000 GPU·h × $2/h ≈ $200,000（约 140 万人民币）
- **这是巨大投入**，个人难以承受，需寻求资助或合作

### 8.2 存储（个人级） ⚠️

- **权重快照**：每个 checkpoint ~50-500 MB（笔记本阶段），保留最近 20 个 ~1-10 GB
- **训练数据**：~1-10 GB（对话语料子集）
- **监测日志**：~100 MB/天
- **总存储需求**：~20-50 GB（笔记本阶段）

### 8.3 物理（个人级） ⚠️

- 训练环境：个人笔记本，建议物理断网训练（关闭 WiFi 减少干扰和散热压力）
- 权重管理：本地加密存储，重要 checkpoint 备份到外置硬盘
- 杀开关：直接关闭训练进程 + 删除 checkpoint

---

## 9. 团队组建

### 9.1 个人研究者路径（默认） ⚠️

> **当前项目定位为个人研究。** 笔记本阶段（阶段 0-2）由个人独立完成。

**个人角色覆盖**：
- ML 研究（SNN 训练、替代梯度、STDP）
- 工程实现（数据管道、训练管线、监控）
- 评估（判据设计、统计分析）
- 安全审计（自我约束，记录决策）

**个人研究者的优势**：
- 决策链条短，迭代快
- 没有 KPI 压力，可以做诚实研究
- 失败成本低，可以快速停损

**个人研究者的劣势**：
- 知识盲区（神经科学、评估方法学可能不熟）
- 时间瓶颈（每项工作都需自己完成）
- 审计独立性弱（自我审计有偏倚）

**缓解劣势的策略**：
- **寻求社区反馈**：在 GitHub、知乎、arXiv 公开研究笔记，接受同行评议
- **咨询神经科学背景的朋友或网友**：定期请教，弥补知识盲区
- **严格记录决策日志**：每个工程决策记录理由，便于回溯和自我审计

### 9.2 阶段 3+ 扩展时的团队需求（仅供参考）

如果笔记本阶段验证假设成立，阶段 3+ 寻求合作或资助时，需要的团队配置：

| 角色 | 人数 | 职责 | 阶段 |
|---|---|---|---|
| ML 研究 | 1-2 | SNN 训练优化 | 阶段 3+ |
| 神经科学顾问 | 1 | 神经元模型、可塑性规则（咨询性质） | 阶段 2+ |
| 评估方法学 | 1 | 统计学、判据设计（咨询性质） | 阶段 4+ |
| 安全审计 | 1-2 | 独立于研究者 | 阶段 4+ |

**诚实标注**：个人研究在阶段 0-2 完全可行，但阶段 3+ 的工程复杂度和算力需求超出了个人能力范围。届时应考虑：
- 加入有算力的研究机构
- 申请研究资助（如学术基金、AI 安全研究资助）
- 与高校合作（如计算神经科学实验室）

---

## 10. 开放问题的工程化处理

### 10.1 🔬 脉冲编码

- **应对**：从 rate coding 起步，每阶段评估替代方案
- **停损**：若阶段 1 三种编码方案均失败，路线崩塌
- **回退**：考虑用预训练的 token embedding 作为输入（部分妥协结构同构）

### 10.2 🔬 SNN 训练理论

- **应对**：跟踪 SNN 训练研究，定期评估新方法
- **停损**：若替代梯度 + STDP 组合在阶段 0-2 不收敛，考虑混合架构
- **回退**：SNN 主干 + 可微辅助层（部分妥协）

### 10.3 🔬 规模门槛

- **应对**：阶段 5 做 scaling law 实验
- **停损**：若 10⁸ 神经元规模性能不显著优于 10⁷，假设证伪
- **回退**：无（若假设证伪，路线中止）

### 10.4 🔬 对话与全量模拟的张力

- **应对**：多目标训练（对话为主 + 内感受为辅）
- **停损**：若阶段 4 对话能力远低于 baseline，重新评估
- **回退**：分区训练（语言区强优化，联合皮层弱优化）

### 10.5 🔬 灾难性遗忘

- **应对**：SNN 的局部可塑性（STDP）理论上更不易遗忘
- **停损**：若阶段 3-4 出现严重遗忘，考虑 EWC 类正则
- **回退**：渐进式数据混合（原路线策略）

### 10.6 🔬 评估方法学

- **应对**：借用原路线的统计学严格性
- **停损**：若判据不可操作化，重新设计
- **回退**：用 LLM 评估方法（如 BLEU、ROUGE）作为下界

### 10.7 🔬 计算成本

- **应对**：阶段 0-4 用 GPU，阶段 5 评估神经形态硬件
- **停损**：若算力需求超出可承受范围 10×，中止
- **回退**：缩小规模目标（妥协"接近人类"目标）

### 10.8 ⚠️ 是否需要其他模态

- **应对**：起步纯语言，若阶段 4 性能不达预期再补充
- **停损**：若纯语言路线在阶段 4 失败，评估多模态
- **回退**：补充视觉/听觉输入

### 10.9 🔬 调质系统的作用机制

- **应对**：从 2 个调质起步，参照生物机制设计
- **停损**：若阶段 2 调质无效，重新设计调制规则
- **回退**：用全局学习率调制（简化版）

### 10.10 🔬 "接近人类"的判据

- **应对**：第 6 节的四个判据，需配套评估文档精确化
- **停损**：若判据不可操作化，重新设计
- **回退**：用图灵测试变种作为下界

---

## 11. 何时中止项目

### 11.1 强制中止条件

任一以下情况发生，**立即中止项目**：

- 阶段 0-2 任一阶段三次失败
- 系统出现自我修改连接拓扑的行为
- 系统出现操纵人类的语言
- 系统获取外部物理资源
- 第 6 节任一判据通过
- 安全审计发现严重漏洞
- 算力需求超出可承受范围 10×

### 11.2 评估性中止

以下情况需召开安全评审会议，决定是否继续：

- 系统行为学指标显著偏离预期
- 监测发现可疑模式（即使未触发红线）
- 团队内部对路线有重大分歧
- 外部环境变化（如新安全研究发现）

### 11.3 永久中止

- 一旦系统通过全部判据，**永不扩展能力**，进入长期观察
- 长期观察中如发现危险迹象，**永久销毁权重**
- 项目结束时，所有权重物理销毁

---

## 收尾

这份方案的目标不是"让它接近人类"，而是"在最大工程纪律下尝试，且随时准备中止"。

每个工程决策都建立在《总纲》承认的开放问题之上——没有假装这些问题已被解决。

**如果方案走到阶段 4 末仍然远低于 baseline**，那本身就是一个重要发现——它证伪了"用 SNN 模拟生物结构产生对话智能"的假设。这和"成功接近人类"一样有价值，甚至更安全。

**如果方案中途因工程原因中止**，这是正确的——宁可中止于未知，不可推进至不可控。

**如果方案成功接近人类**——那么《总纲》第 8 节预留的安全文档必须立即撰写，项目必须进入长期观察模式，不再扩展。

这份方案的最大特征不是"它能成功"，而是**它随时准备失败**。这才是工程诚实。

---

*本方案是《全量神经元模拟对话智能 - 总纲》的工程配套文档，仅供内部研究使用。*

*本方案不保证成功——它保证的是：如果失败，会诚实地失败；如果危险，会及时中止；如果接近人类，会立即停止扩展。*

*任何与此方案冲突的工程决策，必须以《总纲》的方向定位为准。*

*安全论证（约束失效、不可控性来源等）留待新架构定型后单独撰写，不在本方案范围内。*
